---
phase: 03-evidence-and-repair-reasoning
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/domain/repairs.ts
  - src/domain/index.ts
  - src/engine/repair-catalog.ts
  - src/engine/repair-ranking.ts
  - src/engine/repair-generator.ts
  - src/engine/index.ts
  - tests/fixtures/hard-constraint-fixtures.ts
  - tests/engine/repair-generator.test.ts
autonomous: true
requirements: [REPR-01, REPR-02]
user_setup: []
must_haves:
  truths:
    - Repair candidates are generated only from reason-appropriate repair families and remain separate from current validity verdicts.
    - Repair generation searches the failing path locally, deduplicates overlapping candidates, and preserves provenance to source findings.
    - Candidate ranking uses minimal change, then story/world fit, then locality, and exposes only top-three results with confidence bands.
  artifacts:
    - src/domain/repairs.ts
    - src/engine/repair-catalog.ts
    - src/engine/repair-ranking.ts
    - src/engine/repair-generator.ts
    - tests/engine/repair-generator.test.ts
  key_links:
    - src/domain/repairs.ts and src/engine/repair-generator.ts agree on typed repair-object structure and confidence bands.
    - src/engine/repair-catalog.ts constrains which repair families each reason code may produce.
    - tests/engine/repair-generator.test.ts proves local search, top-three output, and repair provenance against canonical fixtures.
---

<objective>
Build the minimal repair reasoning layer that proposes bounded, typed fixes for failing verdicts.

Purpose: Turn explained failures into locally grounded repair options without confusing suggested fixes with current truth.
Output: Repair-domain contracts, reason-scoped generators, ranking/dedup helpers, and repair regressions over canonical fixtures.
</objective>

<execution_context>
@/home/usow/Desktop/storygraph/.codex/get-shit-done/workflows/execute-plan.md
@/home/usow/Desktop/storygraph/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-evidence-and-repair-reasoning/03-CONTEXT.md
@.planning/phases/03-evidence-and-repair-reasoning/03-RESEARCH.md
@.planning/phases/03-evidence-and-repair-reasoning/03-VALIDATION.md
@.planning/phases/03-evidence-and-repair-reasoning/03-01-PLAN.md
@src/domain/verdicts.ts
@src/engine/evidence-snapshots.ts
@src/services/explained-verdicts.ts
@tests/fixtures/hard-constraint-fixtures.ts
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define typed repair contracts and reason-to-family rules</name>
  <files>src/domain/repairs.ts, src/domain/index.ts, src/engine/repair-catalog.ts</files>
  <read_first>.planning/phases/03-evidence-and-repair-reasoning/03-CONTEXT.md, .planning/phases/03-evidence-and-repair-reasoning/03-RESEARCH.md, src/domain/index.ts, src/domain/verdicts.ts, src/engine/reason-codes.ts</read_first>
  <action>Create `src/domain/repairs.ts` and export it from `src/domain/index.ts`. Define schemas named `RepairTypeSchema`, `RepairConfidenceBandSchema`, `RepairCandidateSchema`, and `RepairBundleSchema`. `RepairCandidateSchema` must include the exact fields `repairId`, `repairType`, `reasonCode`, `sourceFindingIds`, `confidenceBand`, `summary`, and a typed `payload`. Create `src/engine/repair-catalog.ts` exporting a reason-to-family map that constrains at least these cases: `missing_causal_link` may emit `add_missing_assumption` and `add_prior_event`; `impossible_travel` may emit `declare_rule` and `add_prior_event`; `character_state_contradiction` may emit `add_missing_assumption` and `add_prior_event`. Do not define any freeform “misc” repair family.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - src/domain/repairs.ts contains `RepairCandidateSchema`.
    - src/domain/repairs.ts contains `RepairConfidenceBandSchema`.
    - src/domain/repairs.ts contains `sourceFindingIds`.
    - src/engine/repair-catalog.ts contains `missing_causal_link`.
    - src/engine/repair-catalog.ts contains `impossible_travel`.
  </acceptance_criteria>
  <done>Phase 3 has explicit repair contracts and reason-scoped family rules instead of ad hoc suggestion objects.</done>
</task>

<task type="auto">
  <name>Task 2: Implement local repair generation, ranking, and deduplication</name>
  <files>src/engine/repair-ranking.ts, src/engine/repair-generator.ts, src/engine/index.ts</files>
  <read_first>.planning/phases/03-evidence-and-repair-reasoning/03-CONTEXT.md, .planning/phases/03-evidence-and-repair-reasoning/03-RESEARCH.md, .planning/phases/03-evidence-and-repair-reasoning/03-01-PLAN.md, src/domain/repairs.ts, src/domain/verdicts.ts, src/engine/repair-catalog.ts, src/services/explained-verdicts.ts</read_first>
  <action>Create `src/engine/repair-ranking.ts` exporting `rankRepairCandidates` and `assignRepairConfidenceBand`. The ranking must sort by exact criteria `minimal change -> story/world fit -> locality`. Create `src/engine/repair-generator.ts` exporting `generateRepairCandidates`. The generator must accept a representative verdict or repairable supporting finding plus its explained evidence, search only the failing path neighborhood, allow a bundle only when the repair catalog marks the reason code as bundle-capable, cap the displayed output to three candidates, and deduplicate overlapping repairs by normalized repair fingerprint while retaining all contributing `sourceFindingIds`. Export the generator surface from `src/engine/index.ts`.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - src/engine/repair-ranking.ts exports `rankRepairCandidates`.
    - src/engine/repair-ranking.ts exports `assignRepairConfidenceBand`.
    - src/engine/repair-generator.ts exports `generateRepairCandidates`.
    - src/engine/repair-generator.ts references `sourceFindingIds`.
    - src/engine/repair-generator.ts contains a top-three cap of `3`.
  </acceptance_criteria>
  <done>Repair candidates can be generated, ranked, and deduplicated deterministically from failing findings.</done>
</task>

<task type="auto">
  <name>Task 3: Add repair-generation regressions over canonical fixtures</name>
  <files>tests/fixtures/hard-constraint-fixtures.ts, tests/engine/repair-generator.test.ts</files>
  <read_first>.planning/phases/03-evidence-and-repair-reasoning/03-VALIDATION.md, tests/fixtures/hard-constraint-fixtures.ts, src/engine/repair-generator.ts, src/services/explained-verdicts.ts</read_first>
  <action>Extend `tests/fixtures/hard-constraint-fixtures.ts` if needed so repair tests can reuse the impossible-travel, missing-cause, betrayal-without-justification, and betrayal-under-threat scenarios. Create `tests/engine/repair-generator.test.ts` and verify these exact behaviors: `missing_causal_link` yields `add_missing_assumption` or `add_prior_event`; `impossible_travel` can yield `declare_rule` or a prior-departure repair; repair output never exceeds three displayed candidates; merged duplicate repairs preserve multiple `sourceFindingIds`; and `notEvaluated` findings do not produce repair candidates of their own.</action>
  <verify>npm run typecheck && npx vitest run tests/engine/repair-generator.test.ts</verify>
  <acceptance_criteria>
    - tests/engine/repair-generator.test.ts contains `missing_causal_link`.
    - tests/engine/repair-generator.test.ts contains `impossible_travel`.
    - tests/engine/repair-generator.test.ts asserts candidate count `3`.
    - tests/engine/repair-generator.test.ts asserts `sourceFindingIds`.
    - tests/engine/repair-generator.test.ts asserts no repairs are generated for `notEvaluated` findings.
  </acceptance_criteria>
  <done>Minimal repair reasoning is executable and regression-tested against canonical failure scenarios.</done>
</task>

</tasks>

<threat_model>
  <trust_boundaries>
    - Failing verdicts cross into repair generation and must not be reinterpreted as canonical truth.
    - Merged repair candidates can originate from multiple findings and must preserve provenance.
  </trust_boundaries>
  <threats>
    <threat>
      <threat_id>T-03-03</threat_id>
      <category>Integrity</category>
      <component>Repair-family selection</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Restrict repair generation by reason-code taxonomy so speculative or unrelated repair families cannot outrank grounded fixes.</mitigation_plan>
    </threat>
    <threat>
      <threat_id>T-03-04</threat_id>
      <category>Repudiation</category>
      <component>Repair deduplication and ranking</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Deduplicate candidates by normalized fingerprint while preserving all source finding IDs and confidence-band derivation so ranked repairs remain auditable.</mitigation_plan>
    </threat>
  </threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] npm run typecheck passes
- [ ] npx vitest run tests/engine/repair-generator.test.ts passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Repair candidates are typed, bounded by reason family, and ranked independently from validity verdicts
- Displayed repairs remain local, provenance-preserving, and capped to three candidates per failing verdict
</success_criteria>

<output>
After completion, create .planning/phases/03-evidence-and-repair-reasoning/03-02-SUMMARY.md
</output>
