---
phase: 05-natural-language-ingestion-and-review-api
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/domain/ingestion.ts
  - src/storage/repositories/ingestion-session-repository.ts
  - src/services/ingestion-review.ts
  - src/services/ingestion-check.ts
  - src/services/index.ts
  - src/api/app.ts
  - src/api/schemas.ts
  - src/api/routes/ingestion-review.ts
  - src/api/routes/ingestion-check.ts
  - package.json
  - tests/storage/ingestion-session-repository.test.ts
  - tests/services/ingestion-review-workflow.test.ts
  - tests/api/ingestion-review-api.test.ts
  - tests/api/check-controls-api.test.ts
autonomous: true
requirements: [FLOW-01, FLOW-03]
user_setup: []
must_haves:
  truths:
    - Writers can apply structured field corrections to review segments while preserving extracted originals and source provenance.
    - Only approved segments promote into canonical story and rule storage, and unapproved segments remain pending review.
    - Consistency checks run only from an explicit check endpoint and reuse the existing verdict runner instead of firing during approval.
  artifacts:
    - src/services/ingestion-review.ts
    - src/services/ingestion-check.ts
    - src/api/routes/ingestion-review.ts
    - src/api/routes/ingestion-check.ts
    - tests/services/ingestion-review-workflow.test.ts
    - tests/api/check-controls-api.test.ts
  key_links:
    - src/services/ingestion-review.ts promotes approved candidate payloads through src/storage/repositories/story-repository.ts, src/storage/repositories/rule-repository.ts, and src/storage/repositories/provenance-repository.ts.
    - src/api/routes/ingestion-review.ts exposes structured patch and segment approval without calling executeVerdictRun.
    - src/services/ingestion-check.ts calls executeVerdictRun only from the explicit check route after the session reaches `approved`.
---

<objective>
Complete the Phase 5 correction loop by adding structured segment patching, approval-gated canonical promotion, explicit workflow transitions, and the manual check surface.

Purpose: Preserve review auditability while letting users promote approved structure into the deterministic reasoning core and trigger checks on demand.
Output: Review patch contracts, approval/promotion orchestration, manual check service and routes, and end-to-end workflow regressions.
</objective>

<execution_context>
@/home/usow/Desktop/storyenginelogic/.codex/get-shit-done/workflows/execute-plan.md
@/home/usow/Desktop/storyenginelogic/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-natural-language-ingestion-and-review-api/05-CONTEXT.md
@.planning/phases/05-natural-language-ingestion-and-review-api/05-RESEARCH.md
@.planning/phases/05-natural-language-ingestion-and-review-api/05-VALIDATION.md
@.planning/phases/05-natural-language-ingestion-and-review-api/05-01-PLAN.md
@.planning/phases/03-evidence-and-repair-reasoning/03-03-SUMMARY.md
@src/domain/ingestion.ts
@src/storage/repositories/ingestion-session-repository.ts
@src/storage/repositories/story-repository.ts
@src/storage/repositories/rule-repository.ts
@src/storage/repositories/provenance-repository.ts
@src/services/verdict-runner.ts
@AGENTS.md

<interfaces>
From src/storage/repositories/story-repository.ts:
```typescript
export interface CanonicalStoryGraph {
  story: StoryRecord;
  revision: StoryRevisionRecord;
  entities: CanonicalEntity[];
  stateBoundaries: CharacterStateBoundary[];
  events: CanonicalEvent[];
  causalLinks: CausalLink[];
}
```

From src/storage/repositories/provenance-repository.ts:
```typescript
export const ProvenanceRecordSchema = z.object({
  provenanceId: ProvenanceIdSchema,
  ownerType: z.enum(["story", "revision", "entity", "state_boundary", "event", "rule", "verdict"]),
  ownerId: z.string().min(1),
  sourceKind: z.enum(["manual", "normalized", "imported", "test_fixture"]),
  sourceRef: z.string().min(1),
  detail: z.record(z.string(), z.unknown()).default({})
});
```

From src/services/verdict-runner.ts:
```typescript
export async function executeVerdictRun(input: {
  storyId: string;
  revisionId: string;
  storyRepository: StoryRepository;
  ruleRepository: RuleRepository;
  verdictRepository: VerdictRepository;
  verdictRunRepository: VerdictRunRepository;
  triggerKind?: "manual" | "rerun" | "test" | "system";
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add structured review patch contracts and approval-state persistence</name>
  <files>src/domain/ingestion.ts, src/storage/repositories/ingestion-session-repository.ts, src/services/ingestion-review.ts, src/services/index.ts, tests/storage/ingestion-session-repository.test.ts</files>
  <read_first>.planning/phases/05-natural-language-ingestion-and-review-api/05-CONTEXT.md, .planning/phases/05-natural-language-ingestion-and-review-api/05-RESEARCH.md, .planning/phases/05-natural-language-ingestion-and-review-api/05-01-PLAN.md, src/domain/ingestion.ts, src/storage/repositories/ingestion-session-repository.ts, tests/storage/ingestion-session-repository.test.ts</read_first>
  <action>Extend `src/domain/ingestion.ts` with the exact schemas `ReviewCandidateCorrectionSchema`, `ReviewSegmentBoundaryPatchSchema`, `ReviewSegmentPatchSchema`, and `SegmentApprovalResultSchema`. `ReviewSegmentBoundaryPatchSchema` must support user-editable `label`, `startOffset`, and `endOffset` fields so D-04 stays structured rather than free-text; `ReviewCandidateCorrectionSchema` must require `candidateId` plus `correctedPayload` and must not introduce free-text annotation channels per D-05. Update `src/storage/repositories/ingestion-session-repository.ts` with the exact methods `applySegmentPatch`, `approveSegment`, `listApprovedSegments`, and `recalculateSessionState`. `applySegmentPatch` must preserve `extracted_payload`, write `corrected_payload` separately, recompute `normalized_payload`, and leave `review_needed` visible for D-15 and D-16. `approveSegment` must approve one segment at a time per D-06, reject any segment whose candidates still have null `normalized_payload`, and compute session states so unapproved segments remain pending review per D-07 and D-08: one or more approved segments but not all segments => `partially_approved`; all segments approved and no check yet => `approved`. Create `src/services/ingestion-review.ts` exporting `applyReviewPatch` and `approveReviewedSegment`, and re-export them from `src/services/index.ts`. Update `tests/storage/ingestion-session-repository.test.ts` so repository coverage proves `correctedPayload` never overwrites `extractedPayload`, boundary edits persist on the segment row, and session-state recomputation produces `partially_approved` only after at least one segment approval.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/domain/ingestion.ts contains `ReviewSegmentPatchSchema`.
    - src/domain/ingestion.ts contains `ReviewSegmentBoundaryPatchSchema`.
    - src/domain/ingestion.ts contains `ReviewCandidateCorrectionSchema`.
    - src/storage/repositories/ingestion-session-repository.ts contains `applySegmentPatch`.
    - src/storage/repositories/ingestion-session-repository.ts contains `approveSegment`.
    - src/storage/repositories/ingestion-session-repository.ts contains `partially_approved`.
    - src/services/ingestion-review.ts exports `applyReviewPatch`.
    - src/services/ingestion-review.ts exports `approveReviewedSegment`.
    - tests/storage/ingestion-session-repository.test.ts contains `correctedPayload`.
    - tests/storage/ingestion-session-repository.test.ts contains `partially_approved`.
  </acceptance_criteria>
  <done>Review patches and segment approvals are stored as structured, auditable state transitions rather than ad hoc JSON updates.</done>
</task>

<task type="auto">
  <name>Task 2: Promote approved segments through existing repositories and add the manual check routes</name>
  <files>src/services/ingestion-review.ts, src/services/ingestion-check.ts, src/services/index.ts, src/api/app.ts, src/api/schemas.ts, src/api/routes/ingestion-review.ts, src/api/routes/ingestion-check.ts</files>
  <read_first>.planning/phases/05-natural-language-ingestion-and-review-api/05-CONTEXT.md, .planning/phases/05-natural-language-ingestion-and-review-api/05-RESEARCH.md, .planning/phases/03-evidence-and-repair-reasoning/03-03-SUMMARY.md, src/storage/repositories/story-repository.ts, src/storage/repositories/rule-repository.ts, src/storage/repositories/provenance-repository.ts, src/services/verdict-runner.ts, src/api/app.ts, src/api/schemas.ts</read_first>
  <action>Extend `src/services/ingestion-review.ts` so `approveReviewedSegment` loads the session snapshot, filters to the exact approved segment, and promotes only approved canonical payloads. For `entity`, `state_boundary`, `event`, and `causal_link` candidates, load the current graph through `StoryRepository.loadGraph` when the target revision exists or initialize a new `CanonicalStoryGraph` using the reserved `story:draft:<sessionId>` and `revision:draft:<sessionId>` IDs from Plan 05-01 when the submission created a new draft session. Merge the approved segment payload into that graph and persist through `StoryRepository.saveGraph`. For `rule` candidates, reuse `RuleRepository.saveRulePack`. For every promoted canonical record, create provenance records through `ProvenanceRepository.saveMany` whose `detail` contains `sessionId`, `segmentId`, `candidateId`, `sourceSpanStart`, `sourceSpanEnd`, `extractedPayload`, and `correctedPayload`, so D-14 and D-15 stay queryable. Create `src/services/ingestion-check.ts` exporting `executeIngestionCheck`; it must reject any session not in `approved`, call `executeVerdictRun` with `triggerKind: "manual"` per D-10 and D-12, store `lastVerdictRunId` and `lastCheckedAt` back on the session record, and move the session state to `checked`. Update `src/api/schemas.ts` with `ReviewSegmentPatchRequestSchema` and `CheckIngestionResponseSchema`. Create `src/api/routes/ingestion-review.ts` with the exact routes `PATCH /api/ingestion/submissions/:sessionId/segments/:segmentId` and `POST /api/ingestion/submissions/:sessionId/segments/:segmentId/approve`, and create `src/api/routes/ingestion-check.ts` with the exact route `POST /api/ingestion/submissions/:sessionId/check`. Register both from `src/api/app.ts`. Keep handlers thin: validate, call services, return updated `workflowState`, and never invoke `executeVerdictRun` from the approval route.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/services/ingestion-review.ts contains `StoryRepository`.
    - src/services/ingestion-review.ts contains `RuleRepository`.
    - src/services/ingestion-review.ts contains `ProvenanceRepository`.
    - src/services/ingestion-review.ts contains `approveReviewedSegment`.
    - src/services/ingestion-check.ts exports `executeIngestionCheck`.
    - src/services/ingestion-check.ts contains `executeVerdictRun`.
    - src/services/ingestion-check.ts contains `checked`.
    - src/api/routes/ingestion-review.ts contains `/api/ingestion/submissions/:sessionId/segments/:segmentId`.
    - src/api/routes/ingestion-review.ts contains `/api/ingestion/submissions/:sessionId/segments/:segmentId/approve`.
    - src/api/routes/ingestion-check.ts contains `/api/ingestion/submissions/:sessionId/check`.
  </acceptance_criteria>
  <done>Approved segments promote into the canonical engine through the existing repositories, and manual checks remain an explicit, separate API action.</done>
</task>

<task type="auto">
  <name>Task 3: Add correction-loop and manual-check regressions plus the ingestion test command</name>
  <files>package.json, tests/services/ingestion-review-workflow.test.ts, tests/api/ingestion-review-api.test.ts, tests/api/check-controls-api.test.ts</files>
  <read_first>.planning/phases/05-natural-language-ingestion-and-review-api/05-VALIDATION.md, src/services/ingestion-review.ts, src/services/ingestion-check.ts, tests/api/ingestion-review-api.test.ts, package.json</read_first>
  <action>Create `tests/services/ingestion-review-workflow.test.ts` to prove structured corrections update `normalizedPayload`, approval promotes only approved segments, and canonical promotion records provenance with both extracted and corrected payloads. Extend `tests/api/ingestion-review-api.test.ts` so it now covers the structured patch and segment-approval routes, including the transitions `needs_review -> partially_approved -> approved`. Create `tests/api/check-controls-api.test.ts` to prove the check route returns `409` before full approval, returns `runId` plus `checked` after approval, and never triggers during submit, extract, patch, or approve. Update `package.json` with a non-watch `test:ingestion` script that runs `tests/storage/ingestion-session-repository.test.ts`, `tests/services/natural-language-extraction.test.ts`, `tests/services/ingestion-review-workflow.test.ts`, `tests/api/ingestion-review-api.test.ts`, and `tests/api/check-controls-api.test.ts` in one command.</action>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/services/ingestion-review-workflow.test.ts tests/api/ingestion-review-api.test.ts tests/api/check-controls-api.test.ts && npm run test:ingestion</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `test:ingestion`.
    - package.json contains `tests/services/ingestion-review-workflow.test.ts`.
    - tests/services/ingestion-review-workflow.test.ts contains `correctedPayload`.
    - tests/services/ingestion-review-workflow.test.ts contains `approveReviewedSegment`.
    - tests/api/ingestion-review-api.test.ts contains `partially_approved` and `approved`.
    - tests/api/check-controls-api.test.ts contains `checked`.
    - tests/api/check-controls-api.test.ts contains `409`.
  </acceptance_criteria>
  <done>The correction loop and manual check controls are protected by deterministic service and Fastify-injection regressions.</done>
</task>

</tasks>

<threat_model>
  <trust_boundaries>
    - Structured review patches cross from the client into the review-session repository and must not erase extracted originals or bypass approval gates.
    - Approved review data crosses from temporary ingestion storage into canonical repositories and verdict execution, so promotion and checking must stay explicit and auditable.
  </trust_boundaries>
  <threats>
    <threat>
      <threat_id>T-05-03</threat_id>
      <category>Tampering</category>
      <component>applySegmentPatch</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Validate patch payloads with `ReviewSegmentPatchSchema`, store `corrected_payload` separately from `extracted_payload`, and recompute `normalized_payload` instead of mutating or replacing extracted source data.</mitigation_plan>
    </threat>
    <threat>
      <threat_id>T-05-04</threat_id>
      <category>Elevation of Privilege</category>
      <component>approveReviewedSegment canonical promotion</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Allow promotion only from segment-level approval, promote only approved segments, and route writes through `StoryRepository`, `RuleRepository`, and `ProvenanceRepository` rather than direct SQL against canonical tables.</mitigation_plan>
    </threat>
    <threat>
      <threat_id>T-05-05</threat_id>
      <category>Repudiation</category>
      <component>POST /api/ingestion/submissions/:sessionId/check</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Keep check as a dedicated route, call `executeVerdictRun` only after `approved`, and persist `lastVerdictRunId`, `lastCheckedAt`, and the explicit `checked` workflow state so approval and checking remain separable in the audit trail.</mitigation_plan>
    </threat>
  </threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] npm run typecheck passes
- [ ] npx vitest run tests/services/ingestion-review-workflow.test.ts tests/api/ingestion-review-api.test.ts tests/api/check-controls-api.test.ts passes
- [ ] npm run test:ingestion passes
</verification>

<success_criteria>
- Structured review patches preserve extracted originals, corrected overlays, and source-span provenance.
- Only approved segments promote into canonical graph/rule storage, and unapproved segments remain pending review.
- Manual consistency checks run only from the explicit check route and reuse `executeVerdictRun`.
</success_criteria>

<output>
After completion, create .planning/phases/05-natural-language-ingestion-and-review-api/05-02-SUMMARY.md
</output>
