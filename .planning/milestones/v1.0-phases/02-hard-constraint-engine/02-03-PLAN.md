---
phase: 02-hard-constraint-engine
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - package.json
  - tests/fixtures/hard-constraint-fixtures.ts
  - tests/engine/regression-physical-temporal.test.ts
  - tests/engine/regression-causality-character.test.ts
  - tests/engine/regression-overrides.test.ts
autonomous: true
requirements: [RULE-01, RULE-02, RULE-03, RULE-04, VERD-01]
user_setup: []
must_haves:
  truths:
    - Deterministic Phase 2 scenarios exist for impossible travel, missing-cause outcomes, unexplained reversals, and valid override-driven exceptions.
    - Regression commands can rerun the hard-engine matrix without relying on watch mode or ad hoc manual test selection.
    - Phase 2 classifications stay stable when the same canonical fixtures are re-evaluated.
  artifacts:
    - package.json
    - tests/fixtures/hard-constraint-fixtures.ts
    - tests/engine/regression-physical-temporal.test.ts
    - tests/engine/regression-causality-character.test.ts
    - tests/engine/regression-overrides.test.ts
  key_links:
    - tests/fixtures/hard-constraint-fixtures.ts feeds the same canonical fixture patterns into all regression suites.
    - package.json exposes a stable engine-focused test command used by validation and later execute-phase verification.
    - The regression suites assert both representative verdicts and supporting evidence semantics from the engine.
---

<objective>
Build the deterministic fixture matrix and regression suite that locks Phase 2 behavior in place.

Purpose: Prevent the hard engine from drifting as later phases add repair reasoning, reruns, and natural-language ingestion.
Output: Shared canonical fixtures, engine regression suites, and stable test commands for Phase 2 behavior.
</objective>

<execution_context>
@/home/usow/Desktop/storygraph/.codex/get-shit-done/workflows/execute-plan.md
@/home/usow/Desktop/storygraph/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hard-constraint-engine/02-CONTEXT.md
@.planning/phases/02-hard-constraint-engine/02-RESEARCH.md
@.planning/phases/02-hard-constraint-engine/02-VALIDATION.md
@.planning/phases/02-hard-constraint-engine/02-01-PLAN.md
@.planning/phases/02-hard-constraint-engine/02-02-PLAN.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reusable hard-constraint fixture builders</name>
  <files>tests/fixtures/hard-constraint-fixtures.ts</files>
  <read_first>.planning/phases/02-hard-constraint-engine/02-RESEARCH.md, .planning/phases/02-hard-constraint-engine/02-02-PLAN.md, tests/storage/persistence.test.ts, tests/canonical/reconstruction.test.ts</read_first>
  <action>Create shared fixture builders in `tests/fixtures/hard-constraint-fixtures.ts` for these exact scenarios: `buildImpossibleTravelFixture`, `buildMissingCauseFixture`, `buildBetrayalWithoutJustificationFixture`, `buildBetrayalUnderThreatFixture`, and `buildRuleOverrideFixture`. Each fixture must return canonical story, revision, entities, boundaries, events, causal links, and relevant rule metadata/version data so the engine tests can evaluate them without bespoke setup code in every file.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - tests/fixtures/hard-constraint-fixtures.ts exports `buildImpossibleTravelFixture`.
    - tests/fixtures/hard-constraint-fixtures.ts exports `buildRuleOverrideFixture`.
    - Each exported fixture returns canonical story graph data plus rule metadata/version data.
  </acceptance_criteria>
  <done>Phase 2 regression scenarios are centralized instead of duplicated across test files.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression suites for physical, causal, character, and override behavior</name>
  <files>tests/engine/regression-physical-temporal.test.ts, tests/engine/regression-causality-character.test.ts, tests/engine/regression-overrides.test.ts</files>
  <read_first>.planning/phases/02-hard-constraint-engine/02-VALIDATION.md, tests/fixtures/hard-constraint-fixtures.ts, src/engine/hard-constraint-engine.ts</read_first>
  <action>Implement three regression suites. `tests/engine/regression-physical-temporal.test.ts` must cover impossible travel, invalid temporal anchors, and physical-rule-blocked movement. `tests/engine/regression-causality-character.test.ts` must cover missing major-outcome causality, unexplained loyalty reversal, and betrayal-under-threat. `tests/engine/regression-overrides.test.ts` must cover baseline reality-default behavior, event-level override winning over story-level default, and inactive local rule staying inactive until explicitly activated. Each suite must assert the representative verdict, supporting finding reason codes, and any `notEvaluated` payload relevant to the scenario.</action>
  <verify>npx vitest run tests/engine/regression-physical-temporal.test.ts tests/engine/regression-causality-character.test.ts tests/engine/regression-overrides.test.ts</verify>
  <acceptance_criteria>
    - tests/engine/regression-physical-temporal.test.ts contains `impossible_travel`.
    - tests/engine/regression-causality-character.test.ts contains `missing_causal_link`.
    - tests/engine/regression-overrides.test.ts contains `event-specific override`.
    - At least one suite asserts supporting finding reason codes in addition to the representative verdict.
  </acceptance_criteria>
  <done>The hard engine’s target contradiction classes are covered by deterministic rerunnable regressions.</done>
</task>

<task type="auto">
  <name>Task 3: Wire stable engine test commands and full-suite verification</name>
  <files>package.json</files>
  <read_first>.planning/phases/02-hard-constraint-engine/02-VALIDATION.md, package.json, tests/engine/regression-physical-temporal.test.ts, tests/engine/regression-causality-character.test.ts, tests/engine/regression-overrides.test.ts</read_first>
  <action>Add a script named `test:engine` to `package.json` that runs `tests/engine/checker-families.test.ts`, `tests/engine/rule-activation.test.ts`, `tests/engine/hard-constraint-engine.test.ts`, `tests/engine/regression-physical-temporal.test.ts`, `tests/engine/regression-causality-character.test.ts`, and `tests/engine/regression-overrides.test.ts` in one non-watch command. Do not replace the existing `test` or `typecheck` scripts. Keep the script shell-compatible with the current npm + Vitest setup.</action>
  <verify>npm run test:engine && npx vitest run</verify>
  <acceptance_criteria>
    - package.json contains `test:engine`.
    - `npm run test:engine` does not use watch mode.
    - `npm run test:engine` covers checker, activation, engine, and regression suites.
  </acceptance_criteria>
  <done>Phase 2 behavior can be rerun quickly and consistently during execution and verification.</done>
</task>

</tasks>

<threat_model>
  <trust_boundaries>
    - Shared fixtures define the canonical scenarios that future phases will trust as regression truth.
    - Validation commands become the enforcement path that guards against classification drift.
  </trust_boundaries>
  <threats>
    <threat>
      <threat_id>T-02-06</threat_id>
      <category>Repudiation</category>
      <component>Regression fixture corpus</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Centralize canonical fixture builders so impossible travel, causal gaps, and character reversals are defined once and reused consistently across all Phase 2 suites.</mitigation_plan>
    </threat>
    <threat>
      <threat_id>T-02-07</threat_id>
      <category>Tampering</category>
      <component>Phase 2 verification commands</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Add a stable `test:engine` command and regression suites so later code changes cannot silently alter verdict classifications without a failing automated check.</mitigation_plan>
    </threat>
  </threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] npm run typecheck passes
- [ ] npm run test:engine passes
- [ ] npx vitest run passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 2 contradiction classes are locked behind deterministic regression fixtures
- A stable engine-only test command exists for ongoing reruns and verification
</success_criteria>

<output>
After completion, create .planning/phases/02-hard-constraint-engine/02-03-SUMMARY.md
</output>
