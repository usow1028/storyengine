---
phase: 02-hard-constraint-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/rules.ts
  - src/domain/verdicts.ts
  - src/domain/index.ts
  - src/storage/migrations/0001_canonical_core.sql
  - src/storage/repositories/rule-repository.ts
  - src/engine/types.ts
  - src/engine/reason-codes.ts
  - src/engine/checkers/time-checker.ts
  - src/engine/checkers/space-checker.ts
  - src/engine/checkers/physics-checker.ts
  - src/engine/checkers/causality-checker.ts
  - src/engine/checkers/character-checker.ts
  - src/engine/index.ts
  - tests/engine/checker-families.test.ts
  - tests/storage/persistence.test.ts
autonomous: true
requirements: [RULE-01, RULE-02, RULE-03, RULE-04, VERD-01]
user_setup: []
must_haves:
  truths:
    - Rule metadata can bind local scopes to a concrete target so precedence and activation are executable instead of heuristic.
    - Each hard-checker family emits stable, reason-coded findings that distinguish Hard Contradiction from Repairable Gap.
    - Verdict evidence can preserve supporting findings and skipped-check metadata without inventing a second verdict format.
  artifacts:
    - src/domain/rules.ts
    - src/domain/verdicts.ts
    - src/engine/types.ts
    - src/engine/reason-codes.ts
    - src/engine/checkers/time-checker.ts
    - src/engine/checkers/space-checker.ts
    - src/engine/checkers/physics-checker.ts
    - src/engine/checkers/causality-checker.ts
    - src/engine/checkers/character-checker.ts
    - tests/engine/checker-families.test.ts
  key_links:
    - src/domain/rules.ts and src/storage/repositories/rule-repository.ts persist the same scope-target contract used by the engine.
    - src/domain/verdicts.ts and src/engine/types.ts agree on reason-coded finding and evidence payload shapes.
    - tests/engine/checker-families.test.ts exercises all five checker families against canonical fixtures.
---

<objective>
Encode the hard-rule families and the execution-ready contracts they depend on.

Purpose: Give Phase 2 a deterministic checker substrate before orchestration or verdict persistence glue is added.
Output: Scope-targeted rule metadata, richer verdict evidence structures, checker-family modules, and family-level regression tests.
</objective>

<execution_context>
@/home/usow/Desktop/storygraph/.codex/get-shit-done/workflows/execute-plan.md
@/home/usow/Desktop/storygraph/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hard-constraint-engine/02-CONTEXT.md
@.planning/phases/02-hard-constraint-engine/02-RESEARCH.md
@.planning/phases/02-hard-constraint-engine/02-VALIDATION.md
@.planning/phases/01-canonical-narrative-schema/01-02-SUMMARY.md
@.planning/phases/01-canonical-narrative-schema/01-03-SUMMARY.md
@src/domain/events.ts
@src/domain/rules.ts
@src/domain/verdicts.ts
@src/services/story-boundary-query.ts
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add execution-ready rule and verdict contracts</name>
  <files>src/domain/rules.ts, src/domain/verdicts.ts, src/domain/index.ts, src/storage/migrations/0001_canonical_core.sql, src/storage/repositories/rule-repository.ts, tests/storage/persistence.test.ts</files>
  <read_first>.planning/phases/02-hard-constraint-engine/02-CONTEXT.md, .planning/phases/02-hard-constraint-engine/02-RESEARCH.md, src/domain/rules.ts, src/domain/verdicts.ts, src/storage/migrations/0001_canonical_core.sql, src/storage/repositories/rule-repository.ts</read_first>
  <action>Extend the rule metadata contract so local scopes can bind to concrete canonical objects. Add an optional `scopeTargetId` field to `RulePackMetadataSchema`, persist it as `scope_target_id` in `rule_packs`, and round-trip it through `RuleRepository` and the persistence fixture. In `src/domain/verdicts.ts`, enrich `VerdictEvidenceSchema` with structured engine payload fields named `representativeChecker`, `reasonCode`, `supportingFindings`, and `notEvaluated`, where supporting findings keep `checker`, `reasonCode`, `category`, `verdictKind`, and canonical evidence IDs. Re-export any new schemas from `src/domain/index.ts` and keep the `verdicts` table unchanged by storing the richer payload inside `evidence` JSONB instead of adding new relational columns.</action>
  <verify>npm run typecheck && npx vitest run tests/storage/persistence.test.ts</verify>
  <acceptance_criteria>
    - src/domain/rules.ts contains `scopeTargetId`.
    - src/storage/migrations/0001_canonical_core.sql contains `scope_target_id`.
    - src/domain/verdicts.ts contains `representativeChecker`, `reasonCode`, `supportingFindings`, and `notEvaluated`.
    - tests/storage/persistence.test.ts asserts the rule repository round-trips `scopeTargetId` and verdict evidence reason codes.
  </acceptance_criteria>
  <done>Rule and verdict contracts can express the exact execution semantics Phase 2 needs.</done>
</task>

<task type="auto">
  <name>Task 2: Implement time, space, and physics checker modules</name>
  <files>src/engine/types.ts, src/engine/reason-codes.ts, src/engine/checkers/time-checker.ts, src/engine/checkers/space-checker.ts, src/engine/checkers/physics-checker.ts, src/engine/index.ts</files>
  <read_first>.planning/phases/02-hard-constraint-engine/02-CONTEXT.md, .planning/phases/02-hard-constraint-engine/02-RESEARCH.md, src/domain/events.ts, src/domain/entities.ts, src/domain/verdicts.ts, src/services/story-boundary-query.ts</read_first>
  <action>Create `src/engine/types.ts` with explicit contracts for `CheckerKind`, `CheckerFinding`, `NotEvaluatedFinding`, and `HardConstraintEvaluation`. Create `src/engine/reason-codes.ts` exporting stable arrays or maps for at least these codes: `invalid_temporal_anchor`, `impossible_travel`, `missing_location_context`, `physical_rule_blocked`, and `rule_override_required`. Implement `runTimeChecks`, `runSpaceChecks`, and `runPhysicsChecks` so they operate on adjacent event pairs, honor `minTravelMinutes`, place hierarchy, and active rule overrides, and classify direct conflicts as `Hard Contradiction` while treating undeclared exceptions as `Repairable Gap`. Export the checker entry points from `src/engine/index.ts`.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - src/engine/types.ts exports `CheckerFinding` and `NotEvaluatedFinding`.
    - src/engine/reason-codes.ts contains `impossible_travel`.
    - src/engine/checkers/time-checker.ts exports `runTimeChecks`.
    - src/engine/checkers/space-checker.ts exports `runSpaceChecks`.
    - src/engine/checkers/physics-checker.ts exports `runPhysicsChecks`.
  </acceptance_criteria>
  <done>Adjacency-based temporal, spatial, and physics contradictions can be evaluated by dedicated checker modules.</done>
</task>

<task type="auto">
  <name>Task 3: Implement causality and character checkers with family-level tests</name>
  <files>src/engine/checkers/causality-checker.ts, src/engine/checkers/character-checker.ts, tests/engine/checker-families.test.ts</files>
  <read_first>.planning/phases/02-hard-constraint-engine/02-CONTEXT.md, .planning/phases/02-hard-constraint-engine/02-RESEARCH.md, src/services/story-boundary-query.ts, src/domain/events.ts, src/domain/state.ts, src/engine/types.ts, src/engine/reason-codes.ts</read_first>
  <action>Implement `runCausalityChecks` and `runCharacterChecks`. `runCausalityChecks` must prefer explicit `causal_links` and only fall back to `preconditions + effects + temporal order + state changes` when those signals jointly support the effect; missing support should emit `Repairable Gap` reason codes such as `missing_causal_link` or `insufficient_state_transition`. `runCharacterChecks` must compare the current action against the immediately prior boundary and extend lineage only for `loyalties`, `goals`, `knowledge`, `conditions`, and `obligations/promises/threats`; if no counter-motive or new condition is present, emit a `character_state_contradiction`, otherwise leave the case repairable. Add `tests/engine/checker-families.test.ts` with fixtures that cover impossible travel, missing-cause major outcome, unexplained betrayal, and betrayal-under-threat.</action>
  <verify>npm run typecheck && npx vitest run tests/engine/checker-families.test.ts</verify>
  <acceptance_criteria>
    - src/engine/checkers/causality-checker.ts exports `runCausalityChecks`.
    - src/engine/checkers/character-checker.ts exports `runCharacterChecks`.
    - tests/engine/checker-families.test.ts contains `missing_causal_link`.
    - tests/engine/checker-families.test.ts contains a betrayal-under-threat case that does not become `Hard Contradiction`.
  </acceptance_criteria>
  <done>All five hard-checker families exist and their direct contradiction vs repairable behavior is executable under test.</done>
</task>

</tasks>

<threat_model>
  <trust_boundaries>
    - Canonical rules cross from passive metadata into executable precedence and override semantics.
    - Checker-family findings become persisted explanatory evidence for downstream repair and review workflows.
  </trust_boundaries>
  <threats>
    <threat>
      <threat_id>T-02-01</threat_id>
      <category>Tampering</category>
      <component>Rule scope activation contract</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Add explicit `scopeTargetId` support to the canonical rule schema and repository so local overrides cannot be activated by heuristic matching.</mitigation_plan>
    </threat>
    <threat>
      <threat_id>T-02-02</threat_id>
      <category>Repudiation</category>
      <component>Checker evidence payload</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Store representative checker, reason codes, supporting findings, and `notEvaluated` metadata inside verdict evidence so short-circuited checks never disappear silently.</mitigation_plan>
    </threat>
  </threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] npm run typecheck passes
- [ ] npx vitest run tests/storage/persistence.test.ts passes
- [ ] npx vitest run tests/engine/checker-families.test.ts passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Rule metadata can target specific events, characters, or locations for deterministic activation
- All checker families emit stable reason-coded findings with explicit evidence
</success_criteria>

<output>
After completion, create .planning/phases/02-hard-constraint-engine/02-01-SUMMARY.md
</output>
