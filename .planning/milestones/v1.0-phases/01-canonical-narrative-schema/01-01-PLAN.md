---
phase: 01-canonical-narrative-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vitest.config.ts
  - src/domain/ids.ts
  - src/domain/entities.ts
  - src/domain/state.ts
  - src/domain/events.ts
  - src/domain/rules.ts
  - src/domain/verdicts.ts
  - src/domain/index.ts
  - tests/canonical/schema.test.ts
autonomous: true
requirements: [MODEL-01, MODEL-02, MODEL-03, MODEL-04]
user_setup: []
must_haves:
  truths:
    - Canonical TypeScript and runtime validation schemas exist for entities, character state, events, world rules, and verdict taxonomies.
    - Stable identifiers and explicit core slots exist for location, vitality, knowledge, goals, loyalties, resources, and extensible typed attributes.
    - Event definitions require causal/state-change semantics rather than prose-only blobs.
  artifacts:
    - package.json
    - tsconfig.json
    - vitest.config.ts
    - src/domain/index.ts
    - tests/canonical/schema.test.ts
  key_links:
    - src/domain/index.ts re-exports all canonical domain modules.
    - tests/canonical/schema.test.ts imports the domain modules and proves the locked Phase 1 schema invariants.
---

<objective>
Establish the canonical domain contract for StoryGraph before any persistence code is written.

Purpose: Lock the explicit data model the later storage, rule, and repair layers will depend on.
Output: A TypeScript-first domain package with runtime validation, stable IDs, and executable schema tests.
</objective>

<execution_context>
@/home/usow/Desktop/storygraph/.codex/get-shit-done/workflows/execute-plan.md
@/home/usow/Desktop/storygraph/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canonical-narrative-schema/01-CONTEXT.md
@.planning/phases/01-canonical-narrative-schema/01-RESEARCH.md
@.planning/phases/01-canonical-narrative-schema/01-VALIDATION.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the canonical schema workspace</name>
  <files>package.json, tsconfig.json, vitest.config.ts</files>
  <read_first>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/phases/01-canonical-narrative-schema/01-CONTEXT.md, .planning/phases/01-canonical-narrative-schema/01-VALIDATION.md</read_first>
  <action>Create a minimal Node.js + TypeScript workspace for the engine. Add package.json scripts named build, test, test:canonical, and typecheck. Configure TypeScript for src and tests directories. Configure Vitest so tests/canonical/*.test.ts can run without watch mode. Use zod for runtime validation and avoid introducing framework or UI dependencies in this phase.</action>
  <verify>npm install && npx vitest run tests/canonical/schema.test.ts --passWithNoTests</verify>
  <acceptance_criteria>
    - package.json defines build, test, test:canonical, and typecheck scripts.
    - tsconfig.json includes src/**/*.ts and tests/**/*.ts.
    - vitest.config.ts runs in one-shot mode and does not enable watch mode.
  </acceptance_criteria>
  <done>Project tooling exists and can execute schema-level tests for the domain package.</done>
</task>

<task type="auto">
  <name>Task 2: Define canonical IDs, entities, state, events, and rules</name>
  <files>src/domain/ids.ts, src/domain/entities.ts, src/domain/state.ts, src/domain/events.ts, src/domain/rules.ts, src/domain/verdicts.ts, src/domain/index.ts</files>
  <read_first>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/phases/01-canonical-narrative-schema/01-CONTEXT.md, .planning/phases/01-canonical-narrative-schema/01-RESEARCH.md</read_first>
  <action>Implement explicit canonical modules for StoryId, RevisionId, EntityId, EventId, RuleId, and VerdictId. Model character, place, object, and world entities as explicit records. In state.ts, create fixed core slots for location, alive status, knowledge, goals, loyalties, resources, and conditions, plus typed extension attributes instead of unbounded blobs. In events.ts, require actors, event type, abstract-event support, temporal relation fields, place, preconditions, effects, and causal links. In rules.ts, separate executable-rule metadata from user-authored source text. In verdicts.ts, define the canonical verdict taxonomy and violation category names used downstream.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - src/domain/state.ts exposes both core-slot and extension-attribute structures.
    - src/domain/events.ts requires at least one state change or rule change link in the canonical event representation.
    - src/domain/rules.ts distinguishes rule metadata from normalized executable form.
    - src/domain/verdicts.ts includes Hard Contradiction, Repairable Gap, Soft Drift, and Consistent.
  </acceptance_criteria>
  <done>The canonical domain model matches the Phase 1 locked decisions and is importable from a single domain index.</done>
</task>

<task type="auto">
  <name>Task 3: Prove schema invariants with executable tests</name>
  <files>tests/canonical/schema.test.ts</files>
  <read_first>.planning/phases/01-canonical-narrative-schema/01-CONTEXT.md, .planning/phases/01-canonical-narrative-schema/01-VALIDATION.md, src/domain/index.ts</read_first>
  <action>Create focused Vitest coverage for the domain package. Assert that canonical entities exist as explicit records, character state exposes the required core slots, canonical events reject definitions without causal or state-change impact, and world rules preserve metadata plus normalized executable representation. Include at least one positive fixture for an abstract event such as betrayal or confession so the phase decision about abstract events is enforced by tests.</action>
  <verify>npx vitest run tests/canonical/schema.test.ts</verify>
  <acceptance_criteria>
    - tests/canonical/schema.test.ts contains both acceptance and rejection cases.
    - The abstract-event fixture passes only when it produces a state or rule change.
    - Test assertions mention the locked core slots and verdict taxonomy by name.
  </acceptance_criteria>
  <done>Schema invariants are executable instead of remaining as prose-only planning decisions.</done>
</task>

</tasks>

<threat_model>
  <trust_boundaries>
    - User-authored narrative assumptions and future normalization output cross into canonical domain validation.
    - Core-slot definitions and verdict taxonomies become a source of truth for later deterministic engines.
  </trust_boundaries>
  <threats>
    <threat>
      <threat_id>T-01-01</threat_id>
      <category>Tampering</category>
      <component>Canonical domain schema</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Use explicit TypeScript types plus runtime validation so malformed entities, states, events, and rules cannot silently enter the canonical model.</mitigation_plan>
    </threat>
    <threat>
      <threat_id>T-01-02</threat_id>
      <category>Repudiation</category>
      <component>Verdict and violation taxonomy naming</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Centralize verdict and violation names in a single exported module and cover them with tests so later phases cannot invent incompatible labels ad hoc.</mitigation_plan>
    </threat>
  </threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] npm run typecheck passes
- [ ] npx vitest run tests/canonical/schema.test.ts passes
- [ ] src/domain/index.ts exports the canonical modules without broken imports
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Canonical records exist for entities, state, events, rules, and verdict taxonomies
- The Phase 1 locked modeling decisions are enforced by executable tests
</success_criteria>

<output>
After completion, create .planning/phases/01-canonical-narrative-schema/01-01-SUMMARY.md
</output>
