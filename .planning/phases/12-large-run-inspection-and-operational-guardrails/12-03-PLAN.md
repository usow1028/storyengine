---
phase: 12-large-run-inspection-and-operational-guardrails
plan: 03
type: execute
wave: 3
depends_on: [12-01, 12-02]
files_modified:
  - tests/services/inspection-payload.test.ts
  - tests/api/inspection-api.test.ts
  - tests/ui/inspection-shell.test.ts
  - tests/browser/inspection-surface.spec.ts
  - tests/browser/inspection-test-server.ts
autonomous: true
requirements: [INSPECT-01, TRACE-01, OPER-01, REVIEW-02]
user_setup: []
must_haves:
  truths:
    - "Mixed-state large-run fixtures prove the inspection console stays usable while still surfacing stale, failed, or unresolved draft state."
    - "Operational guardrail regressions cover service, API, UI, and browser layers so Phase 12 cannot pass on visual inspection alone."
    - "The final Phase 12 gate proves large-run browsing remains deterministic, traceable, and advisory-prior-safe at the v1.1 milestone boundary."
  artifacts:
    - path: "tests/browser/inspection-test-server.ts"
      provides: "Large-run browser fixture with grouped sections, warning counts, and provenance-rich inspection payload"
    - path: "tests/browser/inspection-surface.spec.ts"
      provides: "End-to-end browser validation for grouped/filterable inspection and operational honesty"
    - path: "tests/services/inspection-payload.test.ts"
      provides: "Service-level regression coverage for sanitized guardrail and provenance projections"
    - path: "tests/api/inspection-api.test.ts"
      provides: "API-level regression coverage for additive large-run inspection metadata"
  key_links:
    - from: "tests/browser/inspection-test-server.ts"
      to: "tests/browser/inspection-surface.spec.ts"
      via: "The browser surface test consumes the mixed-state large-run inspection fixture"
      pattern: "Inspection Console"
    - from: "tests/api/inspection-api.test.ts"
      to: "src/api/routes/inspection.ts"
      via: "API regressions prove additive metadata is serialized without leaking raw internals"
      pattern: "operationalSummary"
    - from: "tests/ui/inspection-shell.test.ts"
      to: "src/ui/components/InspectionShell.tsx"
      via: "Static rendering regressions prove grouped triage and warning banners stay wired after refactors"
      pattern: "Verdict Triage"
---

<objective>
Add operational guardrail tests and v1.1 milestone verification artifacts.

Purpose: close Phase 12 with mixed-state regression coverage and a final verification gate that proves the large-run inspection surface is grouped, filterable, traceable, and honest about partial draft state.
Output: reusable large-run fixtures, end-to-end browser/API/UI/service regressions, and a clean verification command matrix for milestone closure.
</objective>

<execution_context>
@/home/usow/Desktop/storygraph/.codex/get-shit-done/workflows/execute-plan.md
@/home/usow/Desktop/storygraph/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-large-run-inspection-and-operational-guardrails/12-CONTEXT.md
@.planning/phases/12-large-run-inspection-and-operational-guardrails/12-RESEARCH.md
@.planning/phases/12-large-run-inspection-and-operational-guardrails/12-VALIDATION.md
@.planning/phases/12-large-run-inspection-and-operational-guardrails/12-01-SUMMARY.md
@.planning/phases/12-large-run-inspection-and-operational-guardrails/12-02-SUMMARY.md
@AGENTS.md

<interfaces>
Current verification surfaces:
```text
GET  /api/inspection/runs/:runId
UI   /inspection/runs/:runId
```

Current automation commands:
```bash
npm exec -- vitest run tests/services/inspection-payload.test.ts tests/api/inspection-api.test.ts tests/ui/inspection-shell.test.ts --bail=1
npm run test:browser -- tests/browser/inspection-surface.spec.ts
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 12-03-01: Build mixed-state large-run fixtures for API and browser guardrail coverage</name>
  <files>tests/services/inspection-payload.test.ts, tests/api/inspection-api.test.ts, tests/browser/inspection-test-server.ts, tests/browser/inspection-surface.spec.ts</files>
  <read_first>
    - .planning/phases/12-large-run-inspection-and-operational-guardrails/12-VALIDATION.md
    - tests/services/inspection-payload.test.ts
    - tests/api/inspection-api.test.ts
    - tests/browser/inspection-test-server.ts
    - tests/browser/inspection-surface.spec.ts
  </read_first>
  <behavior>
    - Fixtures model an inspectable run with grouped section metadata plus unresolved, stale, or failed segment counts.
    - Browser and API tests share a consistent mixed-state payload vocabulary.
    - No fixture leaks raw snapshots or hidden internal fields into the public response.
  </behavior>
  <action>Extend `tests/browser/inspection-test-server.ts` and the API/service tests with a reusable mixed-state large-run fixture that includes section-centered grouping metadata, row/detail provenance, and explicit operational warning counts. Ensure the fixture also covers advisory presence so the final gate can prove hard/soft separation remains intact while the browser shows partial-state honesty.</action>
  <verify>
    <automated>npm exec -- vitest run tests/services/inspection-payload.test.ts tests/api/inspection-api.test.ts --bail=1 && npm run test:browser -- tests/browser/inspection-surface.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "mixed-state|operationalSummary|section" tests/browser/inspection-test-server.ts tests/api/inspection-api.test.ts tests/services/inspection-payload.test.ts` finds the shared large-run fixture coverage.
    - `npm exec -- vitest run tests/services/inspection-payload.test.ts tests/api/inspection-api.test.ts --bail=1 && npm run test:browser -- tests/browser/inspection-surface.spec.ts` passes.
  </acceptance_criteria>
  <done>Mixed-state draft-scale guardrails are now represented in the automated test fixture layer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 12-03-02: Close the full Phase 12 regression gate for grouped and honest inspection</name>
  <files>tests/ui/inspection-shell.test.ts, tests/browser/inspection-surface.spec.ts, tests/services/inspection-payload.test.ts, tests/api/inspection-api.test.ts</files>
  <read_first>
    - tests/ui/inspection-shell.test.ts
    - tests/browser/inspection-surface.spec.ts
    - tests/services/inspection-payload.test.ts
    - tests/api/inspection-api.test.ts
    - .planning/phases/12-large-run-inspection-and-operational-guardrails/12-VALIDATION.md
  </read_first>
  <action>Use the mixed-state fixture to extend final regressions that prove: the warning banner stays visible, global filters preserve verdict-kind order, grouped browsing remains usable, row/detail provenance stays traceable, raw operational internals stay hidden, and advisory signal remains distinct from deterministic verdict truth. Then run the full Phase 12 gate commands from `12-VALIDATION.md` so execution can close with a concrete milestone verification matrix.</action>
  <verify>
    <automated>npm run typecheck && npm run test:reasoning && npm run test:ingestion && npm exec -- vitest run tests/services/inspection-payload.test.ts tests/api/inspection-api.test.ts tests/ui/inspection-shell.test.ts --bail=1 && npm run test:browser -- tests/browser/inspection-surface.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "warning banner|verdict-kind order|provenance|advisory" tests/ui/inspection-shell.test.ts tests/browser/inspection-surface.spec.ts` finds the final guardrail assertions.
    - `npm run typecheck && npm run test:reasoning && npm run test:ingestion && npm exec -- vitest run tests/services/inspection-payload.test.ts tests/api/inspection-api.test.ts tests/ui/inspection-shell.test.ts --bail=1 && npm run test:browser -- tests/browser/inspection-surface.spec.ts` passes.
  </acceptance_criteria>
  <done>The Phase 12 regression gate proves grouped, filterable, traceable, and operationally honest inspection across the full stack.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mixed-state fixture -> API/browser regressions | Shared fixtures define whether operational guardrails are actually tested instead of assumed. |
| automated regression suite -> phase verification | Phase closure depends on concrete automated evidence across service, API, UI, and browser layers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Integrity | mixed-state verification | mitigate | Use fixtures where the run is inspectable but unresolved, stale, or failed counts still exist and must remain visible. |
| T-12-03 | Repudiation | provenance verification | mitigate | Assert row/detail provenance and source-span visibility in service, API, UI, and browser regressions. |
| T-12-04 | Information Disclosure | regression fixture + API output | mitigate | Assert that raw session internals and advisory implementation details are absent from public payloads and rendered UI. |
</threat_model>

<verification>
Run the full Phase 12 gate:

```bash
npm run typecheck
npm run test:reasoning
npm run test:ingestion
npm exec -- vitest run tests/services/inspection-payload.test.ts tests/api/inspection-api.test.ts tests/ui/inspection-shell.test.ts --bail=1
npm run test:browser -- tests/browser/inspection-surface.spec.ts
```
</verification>

<success_criteria>
- Mixed-state fixtures cover grouped large-run browsing and operational warning behavior end to end.
- The final Phase 12 regression gate proves additive payload contracts, UI grouping/filtering, provenance visibility, and hard/soft separation together.
- Phase closure has an explicit automated verification matrix ready for milestone completion artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/12-large-run-inspection-and-operational-guardrails/12-03-SUMMARY.md`.
</output>
