---
phase: 04-corpus-priors-and-soft-pattern-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/priors.ts
  - src/domain/index.ts
  - src/corpus/types.ts
  - src/corpus/normalization.ts
  - src/corpus/prior-build.ts
  - src/corpus/index.ts
  - scripts/build-priors.ts
  - package.json
  - tests/fixtures/corpus-prior-fixtures.ts
  - tests/corpus/corpus-normalization.test.ts
  - tests/corpus/prior-build.test.ts
autonomous: true
requirements: [DATA-02]
user_setup: []
must_haves:
  truths:
    - Corpus works can be normalized into canonical event/state/world-rule exception records rather than freeform corpus-only structures.
    - Offline prior builds produce immutable baseline and genre snapshot exports with explicit world-profile, sample-count, and snapshot-version metadata.
    - DuckDB-backed corpus analysis remains an offline authority while runtime consumption happens through exported snapshots.
  artifacts:
    - src/domain/priors.ts
    - src/corpus/normalization.ts
    - src/corpus/prior-build.ts
    - scripts/build-priors.ts
    - tests/corpus/corpus-normalization.test.ts
    - tests/corpus/prior-build.test.ts
  key_links:
    - src/domain/priors.ts and src/corpus/prior-build.ts agree on snapshot field names and layer semantics.
    - src/corpus/normalization.ts targets the same canonical contracts already defined in src/domain/events.ts, src/domain/state.ts, and src/domain/rules.ts.
    - tests/corpus/prior-build.test.ts proves manual snapshot versioning and baseline/genre exports over deterministic fixtures.
---

<objective>
Build the offline corpus normalization and prior snapshot pipeline that Phase 4 needs before any runtime soft scoring can exist.

Purpose: Give StoryGraph a deterministic way to turn curated narrative corpora into versioned baseline and genre prior artifacts without touching hard verdict behavior yet.
Output: Prior-domain contracts, corpus normalization/build modules, a manual build script, and regression coverage over exported snapshots.
</objective>

<execution_context>
@/home/usow/Desktop/storygraph/.codex/get-shit-done/workflows/execute-plan.md
@/home/usow/Desktop/storygraph/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-corpus-priors-and-soft-pattern-layer/04-CONTEXT.md
@.planning/phases/04-corpus-priors-and-soft-pattern-layer/04-RESEARCH.md
@.planning/phases/04-corpus-priors-and-soft-pattern-layer/04-VALIDATION.md
@.planning/phases/03-evidence-and-repair-reasoning/03-CONTEXT.md
@src/domain/events.ts
@src/domain/state.ts
@src/domain/rules.ts
@src/domain/index.ts
@tests/fixtures/hard-constraint-fixtures.ts
@package.json
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define corpus and prior snapshot contracts</name>
  <files>src/domain/priors.ts, src/domain/index.ts, src/corpus/types.ts, src/corpus/index.ts</files>
  <read_first>.planning/phases/04-corpus-priors-and-soft-pattern-layer/04-CONTEXT.md, .planning/phases/04-corpus-priors-and-soft-pattern-layer/04-RESEARCH.md, src/domain/events.ts, src/domain/state.ts, src/domain/rules.ts, src/domain/index.ts</read_first>
  <action>Create `src/domain/priors.ts` and export it from `src/domain/index.ts`. Define the exact schemas `PriorLayerSchema` with values `baseline` and `genre`, `WorldProfileSchema` with at least `reality-default`, `fantasy-light`, and `sci-fi-override`, `GenreWeightSchema`, `CorpusWorkSchema`, `NormalizedTransitionPatternSchema`, `DriftPatternSchema`, and `PriorSnapshotSchema`. `PriorSnapshotSchema` must contain the exact fields `snapshotId`, `layer`, `genreKey`, `worldProfile`, `sampleCount`, `builtAt`, `sourceWorkIds`, `positivePatterns`, and `driftPatterns`. Create `src/corpus/types.ts` defining typed corpus-input shapes for curated works and scene/event rows, and export those types from `src/corpus/index.ts`.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - src/domain/priors.ts contains `PriorLayerSchema`.
    - src/domain/priors.ts contains `WorldProfileSchema`.
    - src/domain/priors.ts contains `PriorSnapshotSchema`.
    - src/domain/priors.ts contains `positivePatterns` and `driftPatterns`.
    - src/corpus/types.ts contains `CorpusWorkSchema`.
  </acceptance_criteria>
  <done>Phase 4 has explicit contracts for corpus inputs and exported prior snapshots.</done>
</task>

<task type="auto">
  <name>Task 2: Implement canonical corpus normalization and offline snapshot build pipeline</name>
  <files>src/corpus/normalization.ts, src/corpus/prior-build.ts, scripts/build-priors.ts, package.json</files>
  <read_first>.planning/phases/04-corpus-priors-and-soft-pattern-layer/04-CONTEXT.md, .planning/phases/04-corpus-priors-and-soft-pattern-layer/04-RESEARCH.md, src/domain/priors.ts, src/domain/events.ts, src/domain/state.ts, src/domain/rules.ts, package.json</read_first>
  <action>Create `src/corpus/normalization.ts` exporting `normalizeCorpusWork` and `normalizeCorpusTransition`, and normalize curated corpus records into canonical event transitions, state transitions, and world-rule exception tokens. Create `src/corpus/prior-build.ts` exporting `buildPriorSnapshots` and `exportPriorSnapshots`. The builder must produce only `baseline` and `genre` layer snapshots in Phase 4, group by weighted genre key and world profile, track `sampleCount`, and preserve manual snapshot IDs rather than overwriting prior builds. Add `scripts/build-priors.ts` that writes exported snapshots under the exact directory `data/prior-snapshots/` using file names `baseline.prior.json` and `genre-<key>.prior.json`. Update `package.json` with a `build:priors` script that runs `node --loader tsx scripts/build-priors.ts` or the repo-equivalent non-watch command used to generate those exports.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - src/corpus/normalization.ts exports `normalizeCorpusWork`.
    - src/corpus/prior-build.ts exports `buildPriorSnapshots`.
    - scripts/build-priors.ts exists.
    - scripts/build-priors.ts contains `data/prior-snapshots/`.
    - package.json contains `build:priors`.
  </acceptance_criteria>
  <done>StoryGraph can build immutable baseline and genre prior snapshots from canonicalized corpus input.</done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic corpus-build fixtures and regressions</name>
  <files>tests/fixtures/corpus-prior-fixtures.ts, tests/corpus/corpus-normalization.test.ts, tests/corpus/prior-build.test.ts</files>
  <read_first>.planning/phases/04-corpus-priors-and-soft-pattern-layer/04-VALIDATION.md, tests/fixtures/hard-constraint-fixtures.ts, src/corpus/normalization.ts, src/corpus/prior-build.ts</read_first>
  <action>Create `tests/fixtures/corpus-prior-fixtures.ts` with deterministic curated works spanning at least one `reality-default` work and one override-heavy work plus weighted genre labels. Create `tests/corpus/corpus-normalization.test.ts` to prove corpus records normalize into canonical event/state/world-rule exception structures. Create `tests/corpus/prior-build.test.ts` to prove baseline and genre snapshots are emitted separately, snapshot IDs are preserved, `sampleCount` is recorded, and export file names match `baseline.prior.json` plus `genre-<key>.prior.json` conventions.</action>
  <verify>npm run typecheck && npx vitest run tests/corpus/corpus-normalization.test.ts tests/corpus/prior-build.test.ts</verify>
  <acceptance_criteria>
    - tests/corpus/corpus-normalization.test.ts contains `world-rule exception`.
    - tests/corpus/prior-build.test.ts contains `baseline.prior.json`.
    - tests/corpus/prior-build.test.ts contains `sampleCount`.
    - tests/corpus/prior-build.test.ts asserts separate baseline and genre snapshots.
  </acceptance_criteria>
  <done>Offline corpus extraction and snapshot export are regression-tested and ready for runtime use.</done>
</task>

</tasks>

<threat_model>
  <trust_boundaries>
    - Curated corpus records cross into canonical normalization and must not invent semantics that the runtime engine cannot interpret.
    - Offline analytics output crosses into runtime-consumable snapshot exports and must remain versioned and auditable.
  </trust_boundaries>
  <threats>
    <threat>
      <threat_id>T-04-01</threat_id>
      <category>Tampering</category>
      <component>Corpus normalization contract</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Normalize corpus inputs only into canonical event/state/world-rule exception structures and reject ad hoc corpus-only semantics.</mitigation_plan>
    </threat>
    <threat>
      <threat_id>T-04-02</threat_id>
      <category>Repudiation</category>
      <component>Prior snapshot provenance</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Export immutable snapshot IDs, source-work provenance, layer identity, and sample counts so prior builds remain auditable across rebuilds.</mitigation_plan>
    </threat>
  </threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] npm run typecheck passes
- [ ] npx vitest run tests/corpus/corpus-normalization.test.ts tests/corpus/prior-build.test.ts passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Corpus ingestion and analysis can build baseline and genre prior snapshots offline
- Prior snapshots preserve world-profile, sample-count, and snapshot-version metadata
</success_criteria>

<output>
After completion, create .planning/phases/04-corpus-priors-and-soft-pattern-layer/04-01-SUMMARY.md
</output>
