---
phase: 04-corpus-priors-and-soft-pattern-layer
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/domain/priors.ts
  - src/domain/index.ts
  - src/engine/types.ts
  - src/engine/prior-snapshot-loader.ts
  - src/engine/soft-prior-scoring.ts
  - src/engine/repair-plausibility.ts
  - src/engine/index.ts
  - src/services/soft-prior-evaluator.ts
  - src/services/index.ts
  - package.json
  - tests/fixtures/soft-prior-fixtures.ts
  - tests/engine/soft-prior-scoring.test.ts
  - tests/engine/repair-plausibility.test.ts
autonomous: true
requirements: [SOFT-01, DATA-02]
user_setup: []
must_haves:
  truths:
    - Runtime soft-prior evaluation loads baseline and genre snapshots, records layer contributions, and emits decomposed drift-type scores with dynamic thresholds.
    - Sparse genre/world layers weaken rather than disappear abruptly, and backoff keeps the baseline layer available.
    - Repair candidates can be reranked by prior plausibility without changing hard verdict kinds or hard-checker severity.
  artifacts:
    - src/engine/prior-snapshot-loader.ts
    - src/engine/soft-prior-scoring.ts
    - src/engine/repair-plausibility.ts
    - src/services/soft-prior-evaluator.ts
    - tests/engine/soft-prior-scoring.test.ts
    - tests/engine/repair-plausibility.test.ts
  key_links:
    - src/engine/prior-snapshot-loader.ts and src/domain/priors.ts agree on snapshot field names and layer metadata.
    - src/engine/soft-prior-scoring.ts emits score structures that preserve dominant-layer and contribution detail defined in src/domain/priors.ts.
    - src/engine/repair-plausibility.ts adjusts repair ordering only, leaving hard verdict kinds untouched and provable through tests.
---

<objective>
Implement the runtime soft-prior layer that scores drift and reranks repairs using corpus-derived evidence.

Purpose: Let StoryGraph use corpus-backed priors as advisory signals without ever promoting them to hard law.
Output: Snapshot loaders, decomposed soft scoring, repair plausibility reranking, and runtime regressions.
</objective>

<execution_context>
@/home/usow/Desktop/storygraph/.codex/get-shit-done/workflows/execute-plan.md
@/home/usow/Desktop/storygraph/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-corpus-priors-and-soft-pattern-layer/04-CONTEXT.md
@.planning/phases/04-corpus-priors-and-soft-pattern-layer/04-RESEARCH.md
@.planning/phases/04-corpus-priors-and-soft-pattern-layer/04-VALIDATION.md
@.planning/phases/04-corpus-priors-and-soft-pattern-layer/04-01-PLAN.md
@src/domain/priors.ts
@src/domain/repairs.ts
@src/engine/repair-ranking.ts
@src/services/explained-verdicts.ts
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define runtime soft-prior assessment contracts and snapshot loading surface</name>
  <files>src/domain/priors.ts, src/domain/index.ts, src/engine/types.ts, src/engine/prior-snapshot-loader.ts, src/engine/index.ts</files>
  <read_first>.planning/phases/04-corpus-priors-and-soft-pattern-layer/04-CONTEXT.md, .planning/phases/04-corpus-priors-and-soft-pattern-layer/04-RESEARCH.md, .planning/phases/04-corpus-priors-and-soft-pattern-layer/04-01-PLAN.md, src/domain/priors.ts, src/engine/types.ts, src/engine/index.ts</read_first>
  <action>Extend `src/domain/priors.ts` with runtime scoring contracts named `SoftDriftTypeSchema`, `PriorContributionSchema`, `SoftPriorAssessmentSchema`, and `RepairPlausibilityAdjustmentSchema`. `SoftDriftTypeSchema` must include the exact values `transition_drift`, `motivation_drift`, and `rule_exception_rarity`. `SoftPriorAssessmentSchema` must include `driftScores`, `dominantPriorLayer`, `triggeredDrifts`, and `representativePatternSummary`. Create `src/engine/prior-snapshot-loader.ts` exporting `loadPriorSnapshotSet`, and make it load the exact snapshot files `baseline.prior.json` and `genre-<key>.prior.json` from the snapshot export directory. Update `src/engine/types.ts` and `src/engine/index.ts` so the loader and runtime assessment types are exported cleanly.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - src/domain/priors.ts contains `SoftDriftTypeSchema`.
    - src/domain/priors.ts contains `transition_drift`.
    - src/domain/priors.ts contains `dominantPriorLayer`.
    - src/engine/prior-snapshot-loader.ts exports `loadPriorSnapshotSet`.
    - src/engine/prior-snapshot-loader.ts references `baseline.prior.json`.
  </acceptance_criteria>
  <done>Phase 4 has explicit runtime contracts for soft drift scoring and prior snapshot loading.</done>
</task>

<task type="auto">
  <name>Task 2: Implement layered soft scoring and repair plausibility reranking</name>
  <files>src/engine/soft-prior-scoring.ts, src/engine/repair-plausibility.ts, src/services/soft-prior-evaluator.ts, src/services/index.ts</files>
  <read_first>.planning/phases/04-corpus-priors-and-soft-pattern-layer/04-CONTEXT.md, .planning/phases/04-corpus-priors-and-soft-pattern-layer/04-RESEARCH.md, src/domain/priors.ts, src/domain/repairs.ts, src/engine/repair-ranking.ts, src/services/explained-verdicts.ts</read_first>
  <action>Create `src/engine/soft-prior-scoring.ts` exporting `scoreSoftDrift`. The scorer must evaluate baseline and genre layers only in Phase 4, compute decomposed scores for `transition_drift`, `motivation_drift`, and `rule_exception_rarity`, lower the impact of sparse layers using sample-count-aware confidence, and apply dynamic thresholds rather than one fixed threshold. Create `src/engine/repair-plausibility.ts` exporting `rerankRepairsWithPriors`, and rerank repair candidates by plausibility using the soft-prior assessment while leaving each candidate's hard verdict context unchanged. Create `src/services/soft-prior-evaluator.ts` to orchestrate snapshot loading, soft scoring, and repair reranking for a given event/state transition plus repair list, then re-export the service from `src/services/index.ts`.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - src/engine/soft-prior-scoring.ts exports `scoreSoftDrift`.
    - src/engine/soft-prior-scoring.ts contains `transition_drift`, `motivation_drift`, and `rule_exception_rarity`.
    - src/engine/soft-prior-scoring.ts contains `dominantPriorLayer`.
    - src/engine/repair-plausibility.ts exports `rerankRepairsWithPriors`.
    - src/engine/repair-plausibility.ts does not assign or mutate `verdictKind`.
  </acceptance_criteria>
  <done>StoryGraph can compute layered soft drift signals and use them to rerank repairs without changing hard truth.</done>
</task>

<task type="auto">
  <name>Task 3: Add soft-prior runtime regressions and a dedicated prior test command</name>
  <files>tests/fixtures/soft-prior-fixtures.ts, tests/engine/soft-prior-scoring.test.ts, tests/engine/repair-plausibility.test.ts, package.json</files>
  <read_first>.planning/phases/04-corpus-priors-and-soft-pattern-layer/04-VALIDATION.md, tests/fixtures/corpus-prior-fixtures.ts, src/engine/soft-prior-scoring.ts, src/engine/repair-plausibility.ts, package.json</read_first>
  <action>Create `tests/fixtures/soft-prior-fixtures.ts` with deterministic baseline-vs-genre and sparse-layer scenarios. Create `tests/engine/soft-prior-scoring.test.ts` proving drift-type scores are emitted separately, sparse genre layers weaken with lower confidence, dominant-layer attribution is recorded, and dynamic thresholds prevent weak evidence from triggering the same way as strong evidence. Create `tests/engine/repair-plausibility.test.ts` proving prior evidence reranks repairs by plausibility while preserving the original hard verdict kind. Update `package.json` with a `test:priors` script that runs `tests/corpus/corpus-normalization.test.ts`, `tests/corpus/prior-build.test.ts`, `tests/engine/soft-prior-scoring.test.ts`, and `tests/engine/repair-plausibility.test.ts` without watch mode.</action>
  <verify>npm run typecheck && npm run test:priors</verify>
  <acceptance_criteria>
    - tests/engine/soft-prior-scoring.test.ts contains `dominant prior layer`.
    - tests/engine/soft-prior-scoring.test.ts contains `dynamic threshold`.
    - tests/engine/repair-plausibility.test.ts asserts hard verdict kinds stay unchanged.
    - package.json contains `test:priors`.
    - `test:priors` does not use watch mode.
  </acceptance_criteria>
  <done>Soft-prior scoring and repair reranking are protected by deterministic runtime regressions.</done>
</task>

</tasks>

<threat_model>
  <trust_boundaries>
    - Offline prior snapshots cross into runtime scoring and must not acquire authority equal to hard verdicts.
    - Soft drift and repair plausibility outputs cross into user-facing reasoning surfaces and therefore must remain explainable.
  </trust_boundaries>
  <threats>
    <threat>
      <threat_id>T-04-03</threat_id>
      <category>Elevation of Privilege</category>
      <component>Soft-prior verdict influence</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Keep soft-prior scoring separate from hard verdict kinds and allow priors to affect only soft drift output plus repair reranking.</mitigation_plan>
    </threat>
    <threat>
      <threat_id>T-04-04</threat_id>
      <category>Repudiation</category>
      <component>Soft-prior evidence opacity</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Record dominant layer, contribution breakdown, representative pattern summary, and confidence-sensitive thresholds for every soft assessment.</mitigation_plan>
    </threat>
  </threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] npm run typecheck passes
- [ ] npx vitest run tests/engine/soft-prior-scoring.test.ts tests/engine/repair-plausibility.test.ts passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Soft priors produce advisory drift signals without changing hard verdicts
- Repair candidates can be reranked by plausibility with layer-aware evidence and dynamic thresholds
</success_criteria>

<output>
After completion, create .planning/phases/04-corpus-priors-and-soft-pattern-layer/04-02-SUMMARY.md
</output>
