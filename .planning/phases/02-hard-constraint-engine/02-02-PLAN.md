---
phase: 02-hard-constraint-engine
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/engine/rule-activation.ts
  - src/engine/verdict-aggregator.ts
  - src/engine/hard-constraint-engine.ts
  - src/engine/index.ts
  - src/services/index.ts
  - src/storage/repositories/rule-repository.ts
  - src/storage/repositories/verdict-repository.ts
  - tests/engine/rule-activation.test.ts
  - tests/engine/hard-constraint-engine.test.ts
autonomous: true
requirements: [RULE-01, RULE-02, RULE-03, RULE-04, VERD-01]
user_setup: []
must_haves:
  truths:
    - The engine resolves active rules deterministically using fixed scope precedence plus same-scope priority.
    - The engine runs checker families in the locked order and records skipped downstream checks explicitly when an upstream contradiction blocks them.
    - One representative verdict plus supporting findings can be produced and persisted for an evaluated path or revision.
  artifacts:
    - src/engine/rule-activation.ts
    - src/engine/verdict-aggregator.ts
    - src/engine/hard-constraint-engine.ts
    - src/storage/repositories/rule-repository.ts
    - src/storage/repositories/verdict-repository.ts
    - tests/engine/rule-activation.test.ts
    - tests/engine/hard-constraint-engine.test.ts
  key_links:
    - src/engine/rule-activation.ts consumes rule metadata/version rows from src/storage/repositories/rule-repository.ts.
    - src/engine/hard-constraint-engine.ts calls the family checkers in the locked order and delegates verdict selection to src/engine/verdict-aggregator.ts.
    - tests/engine/hard-constraint-engine.test.ts proves representative verdict selection and skipped-check metadata against canonical fixtures.
---

<objective>
Turn the checker families into an executable hard-constraint engine with deterministic rule activation and verdict aggregation.

Purpose: Move Phase 2 from isolated checker modules to an engine that can evaluate a story revision or path end-to-end.
Output: Rule-activation resolver, hard engine orchestrator, verdict aggregation helpers, repository integration, and orchestration tests.
</objective>

<execution_context>
@/home/usow/Desktop/storygraph/.codex/get-shit-done/workflows/execute-plan.md
@/home/usow/Desktop/storygraph/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hard-constraint-engine/02-CONTEXT.md
@.planning/phases/02-hard-constraint-engine/02-RESEARCH.md
@.planning/phases/02-hard-constraint-engine/02-VALIDATION.md
@.planning/phases/02-hard-constraint-engine/02-01-PLAN.md
@.planning/phases/01-canonical-narrative-schema/01-02-SUMMARY.md
@.planning/phases/01-canonical-narrative-schema/01-03-SUMMARY.md
@src/services/snapshot-rebuilder.ts
@src/services/story-boundary-query.ts
@src/storage/repositories/rule-repository.ts
@src/storage/repositories/verdict-repository.ts
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement rule activation and precedence resolution</name>
  <files>src/engine/rule-activation.ts, src/storage/repositories/rule-repository.ts, tests/engine/rule-activation.test.ts</files>
  <read_first>.planning/phases/02-hard-constraint-engine/02-CONTEXT.md, .planning/phases/02-hard-constraint-engine/02-RESEARCH.md, .planning/phases/02-hard-constraint-engine/02-01-PLAN.md, src/domain/rules.ts, src/domain/events.ts, src/storage/repositories/rule-repository.ts</read_first>
  <action>Add repository methods that can list all rule metadata and versions for a story revision, not only load a single rule version. In `src/engine/rule-activation.ts`, implement `resolveActiveRuleSet` with these exact rules: baseline `global` and `story` rules start active when `active=true`; `location`, `character`, and `event` rules require either matching `scopeTargetId` or an explicit `activate`, `override`, or `declare` event rule change; scope precedence is `event > character > location > story > global`; ties inside the same scope are broken by descending numeric `priority`. Add tests that prove an event-specific override beats a story default, a location rule beats a story default, and an inactive local rule stays inactive until activated.</action>
  <verify>npm run typecheck && npx vitest run tests/engine/rule-activation.test.ts</verify>
  <acceptance_criteria>
    - src/storage/repositories/rule-repository.ts exports a revision-level rule listing method.
    - src/engine/rule-activation.ts exports `resolveActiveRuleSet`.
    - tests/engine/rule-activation.test.ts contains `event-specific override beats story default`.
    - tests/engine/rule-activation.test.ts contains `inactive local rule stays inactive`.
  </acceptance_criteria>
  <done>The engine can compute a deterministic active rule snapshot for any evaluated event or path.</done>
</task>

<task type="auto">
  <name>Task 2: Build the hard engine orchestration and verdict aggregation flow</name>
  <files>src/engine/verdict-aggregator.ts, src/engine/hard-constraint-engine.ts, src/engine/index.ts, src/services/index.ts, src/storage/repositories/verdict-repository.ts</files>
  <read_first>.planning/phases/02-hard-constraint-engine/02-CONTEXT.md, .planning/phases/02-hard-constraint-engine/02-RESEARCH.md, src/engine/types.ts, src/engine/reason-codes.ts, src/services/story-boundary-query.ts, src/storage/repositories/verdict-repository.ts</read_first>
  <action>Create `src/engine/verdict-aggregator.ts` with a function named `aggregateFindings` that selects one representative verdict using this severity order: `Hard Contradiction > Repairable Gap > Soft Drift > Consistent`. Create `src/engine/hard-constraint-engine.ts` with entry points named `evaluateEventPath` and `evaluateRevision`. The engine must call `resolveActiveRuleSet` first, then run checker families in this exact order: `time`, `space`, `physics`, `causality`, `character`. If a `Hard Contradiction` from time/space/physics invalidates downstream assumptions, append `notEvaluated` entries for the skipped families instead of running them. Update `VerdictRepository` so it can persist multiple verdict records for a revision via a method such as `saveMany` or `replaceForRevision`, and export the engine surface through `src/engine/index.ts` and `src/services/index.ts`.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - src/engine/verdict-aggregator.ts exports `aggregateFindings`.
    - src/engine/hard-constraint-engine.ts exports `evaluateEventPath` and `evaluateRevision`.
    - src/engine/hard-constraint-engine.ts runs checkers in the order `time`, `space`, `physics`, `causality`, `character`.
    - src/storage/repositories/verdict-repository.ts supports saving multiple verdict records for one revision.
  </acceptance_criteria>
  <done>The project has an executable hard-constraint engine instead of isolated checker utilities.</done>
</task>

<task type="auto">
  <name>Task 3: Prove deterministic orchestration, short-circuiting, and representative verdict selection</name>
  <files>tests/engine/hard-constraint-engine.test.ts</files>
  <read_first>.planning/phases/02-hard-constraint-engine/02-VALIDATION.md, src/engine/hard-constraint-engine.ts, src/engine/verdict-aggregator.ts, src/services/index.ts</read_first>
  <action>Create orchestration tests that feed canonical fixtures into the engine and assert all of the following: impossible travel produces a representative `Hard Contradiction`; downstream causality and character checks appear in `notEvaluated` when they are skipped by an upstream hard contradiction; a betrayal-under-threat scenario yields a representative `Repairable Gap` or non-hard outcome instead of a hard contradiction; and rule overrides change the representative verdict deterministically when the active rule set changes. Keep the tests focused on engine output, not raw checker internals.</action>
  <verify>npm run typecheck && npx vitest run tests/engine/rule-activation.test.ts tests/engine/hard-constraint-engine.test.ts</verify>
  <acceptance_criteria>
    - tests/engine/hard-constraint-engine.test.ts asserts `notEvaluated`.
    - tests/engine/hard-constraint-engine.test.ts asserts representative verdict severity ordering.
    - tests/engine/hard-constraint-engine.test.ts includes a rule override case that changes the verdict deterministically.
  </acceptance_criteria>
  <done>Rule activation, engine ordering, and representative verdict semantics are executable and regression-tested.</done>
</task>

</tasks>

<threat_model>
  <trust_boundaries>
    - Stored rule metadata and event rule changes cross into the engine’s effective-rule computation.
    - Persisted verdict records become the canonical output for later explanation and review phases.
  </trust_boundaries>
  <threats>
    <threat>
      <threat_id>T-02-03</threat_id>
      <category>Tampering</category>
      <component>Rule activation resolver</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Compute effective rules from persisted scope, target, activation, and event rule changes so overrides cannot activate outside their intended boundary.</mitigation_plan>
    </threat>
    <threat>
      <threat_id>T-02-04</threat_id>
      <category>Elevation of Privilege</category>
      <component>Scope precedence engine</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Apply the fixed scope chain before same-scope priority so story defaults cannot overrule event- or character-specific exceptions.</mitigation_plan>
    </threat>
    <threat>
      <threat_id>T-02-05</threat_id>
      <category>Repudiation</category>
      <component>Representative verdict aggregation</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Persist supporting findings and skipped-check metadata together with the representative verdict so downstream consumers can audit what the engine actually evaluated.</mitigation_plan>
    </threat>
  </threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] npm run typecheck passes
- [ ] npx vitest run tests/engine/rule-activation.test.ts passes
- [ ] npx vitest run tests/engine/hard-constraint-engine.test.ts passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Active rule resolution is deterministic across scope precedence and same-scope priority
- The hard engine returns representative verdicts with supporting findings and explicit skipped-check metadata
</success_criteria>

<output>
After completion, create .planning/phases/02-hard-constraint-engine/02-02-SUMMARY.md
</output>
