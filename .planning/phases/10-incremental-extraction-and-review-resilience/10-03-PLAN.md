---
phase: 10-incremental-extraction-and-review-resilience
plan: 03
type: execute
wave: 3
depends_on: [10-01, 10-02]
files_modified:
  - package.json
  - tests/services/incremental-extraction-workflow.test.ts
  - tests/services/ingestion-review-workflow.test.ts
  - tests/storage/ingestion-session-repository.test.ts
  - tests/api/ingestion-review-api.test.ts
  - tests/api/check-controls-api.test.ts
autonomous: true
requirements: [DRAFT-01, DRAFT-04, REVIEW-01, REVIEW-02, OPER-01]
user_setup: []
must_haves:
  truths:
    - "Multi-segment draft sessions can succeed, fail, retry, reopen approval, and reapprove without corrupting untouched segments."
    - "Attempt history and progress summaries remain visible across repository, service, and API surfaces."
    - "The standard ingestion workflow preserves incremental retry, approval-reset, and full-session check-block behavior end to end."
  artifacts:
    - path: "tests/services/incremental-extraction-workflow.test.ts"
      provides: "Cross-segment service lifecycle matrix for success, failure, retry, and approval reset"
    - path: "tests/api/check-controls-api.test.ts"
      provides: "Public workflow proof that reopened segments still block the unchanged full-session /check route"
    - path: "package.json"
      provides: "Ingestion test script updated to include the incremental extraction lifecycle file"
  key_links:
    - from: "package.json"
      to: "tests/services/incremental-extraction-workflow.test.ts"
      via: "test:ingestion runs the new lifecycle file in the standard gate"
      pattern: "incremental-extraction-workflow.test.ts"
    - from: "tests/api/ingestion-review-api.test.ts"
      to: "src/api/routes/ingestion-extract.ts"
      via: "Selected retry and approval-reset flow uses the public /extract surface"
      pattern: "allowApprovalReset"
    - from: "tests/storage/ingestion-session-repository.test.ts"
      to: "src/storage/repositories/ingestion-session-repository.ts"
      via: "Attempt rows stay ordered by attemptNumber while latest snapshot data remains current"
      pattern: "attemptNumber"
---

<objective>
Add the multi-segment lifecycle regression matrix and close the Phase 10 automated validation loop.

Purpose: Convert the new extraction, retry, approval-reset, provenance, and full-session check rules into durable cross-layer proofs that stay inside the normal ingestion test gate.
Output: Expanded service, repository, and API lifecycle tests plus `test:ingestion` coverage for the new incremental workflow file.
</objective>

<execution_context>
@/home/usow/Desktop/storygraph/.codex/get-shit-done/workflows/execute-plan.md
@/home/usow/Desktop/storygraph/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-incremental-extraction-and-review-resilience/10-CONTEXT.md
@.planning/phases/10-incremental-extraction-and-review-resilience/10-RESEARCH.md
@.planning/phases/10-incremental-extraction-and-review-resilience/10-VALIDATION.md
@.planning/phases/10-incremental-extraction-and-review-resilience/10-01-SUMMARY.md
@.planning/phases/10-incremental-extraction-and-review-resilience/10-02-SUMMARY.md
@AGENTS.md

<interfaces>
Workflow paths to prove end to end:
```text
POST   /api/ingestion/submissions/:sessionId/extract
PATCH  /api/ingestion/submissions/:sessionId/segments/:segmentId
POST   /api/ingestion/submissions/:sessionId/segments/:segmentId/approve
POST   /api/ingestion/submissions/:sessionId/check
```

Current gate to extend:
```json
// package.json
"test:ingestion": "vitest run tests/storage/ingestion-session-repository.test.ts tests/services/natural-language-extraction.test.ts tests/services/ingestion-review-workflow.test.ts tests/api/ingestion-review-api.test.ts tests/api/check-controls-api.test.ts"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 10-03-01: Add service lifecycle coverage for mixed success, failure, retry, and reapproval</name>
  <files>tests/services/incremental-extraction-workflow.test.ts, tests/services/ingestion-review-workflow.test.ts</files>
  <read_first>
    - tests/services/incremental-extraction-workflow.test.ts
    - tests/services/ingestion-review-workflow.test.ts
    - src/services/ingestion-session.ts
    - src/services/ingestion-review.ts
    - src/storage/repositories/ingestion-session-repository.ts
    - .planning/phases/10-incremental-extraction-and-review-resilience/10-VALIDATION.md
  </read_first>
  <behavior>
    - A three-segment workflow covers one immediate success, one failed retry followed by a successful retry, and one approved segment reopened with `allowApprovalReset: true` and later reapproved.
    - Attempt numbers increase per segment and the failed-then-successful segment keeps ordered lineage `["success", "failed", "success"]`.
    - No-op reapproval leaves provenance counts unchanged, while reset-based reapproval records the updated extracted/corrected payload once.
  </behavior>
  <action>Extend `tests/services/incremental-extraction-workflow.test.ts` with `runs a three-segment lifecycle with mixed success failure retry and reapproval` using sessionId `session:lifecycle`, storyId `story:lifecycle`, revisionId `revision:lifecycle:1`, and segment IDs `segment:session:lifecycle:1`, `segment:session:lifecycle:2`, and `segment:session:lifecycle:3`. Configure the fake extractor so segment 2 throws once, segment 3 is approved before reset, and the retry of segment 3 uses `allowApprovalReset: true`. Extend `tests/services/ingestion-review-workflow.test.ts` with `keeps provenance stable across no-op reapproval and reset-based reapproval`. Assert concrete values: segment 2 attempt statuses equal `["success", "failed", "success"]`, segment 3 clears approval during reset then returns to `approved`, and provenance counts for unchanged no-op reapproval do not increase.</action>
  <verify>
    <automated>npm exec -- vitest run tests/services/incremental-extraction-workflow.test.ts tests/services/ingestion-review-workflow.test.ts --bail=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "runs a three-segment lifecycle with mixed success failure retry and reapproval" tests/services/incremental-extraction-workflow.test.ts` finds the service lifecycle matrix.
    - `rg -n "keeps provenance stable across no-op reapproval and reset-based reapproval" tests/services/ingestion-review-workflow.test.ts` finds the provenance stability regression.
    - `rg -n "\\[\\\"success\\\", \\\"failed\\\", \\\"success\\\"\\]|allowApprovalReset" tests/services/incremental-extraction-workflow.test.ts` finds the concrete lineage assertions.
    - `npm exec -- vitest run tests/services/incremental-extraction-workflow.test.ts tests/services/ingestion-review-workflow.test.ts --bail=1` passes.
  </acceptance_criteria>
  <done>Service-level lifecycle behavior is pinned for multi-segment retry, approval reset, and provenance stability.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 10-03-02: Extend repository and suite-level lifecycle coverage</name>
  <files>package.json, tests/storage/ingestion-session-repository.test.ts, tests/services/incremental-extraction-workflow.test.ts</files>
  <read_first>
    - package.json
    - tests/storage/ingestion-session-repository.test.ts
    - tests/services/incremental-extraction-workflow.test.ts
    - src/storage/repositories/ingestion-session-repository.ts
    - .planning/phases/10-incremental-extraction-and-review-resilience/10-VALIDATION.md
  </read_first>
  <action>Extend `tests/storage/ingestion-session-repository.test.ts` with a regression named `loads attempt history ordered by attemptNumber and preserves the latest snapshot after mixed retries`. Build a repository fixture where one segment succeeds, then fails, then succeeds again, and assert `attempts.map((attempt) => attempt.attemptNumber)` equals `[1, 2, 3]`, `attempts[1].status === "failed"`, `segment.lastAttemptStatus === "success"`, and the current `candidates` still match the third attempt snapshot. In `package.json`, update `test:ingestion` to include `tests/services/incremental-extraction-workflow.test.ts` directly after `tests/services/natural-language-extraction.test.ts` so the new lifecycle file runs in the normal ingestion gate. Do not add watch mode, shell globs, or separate one-off scripts.</action>
  <verify>
    <automated>npm exec -- vitest run tests/storage/ingestion-session-repository.test.ts tests/services/incremental-extraction-workflow.test.ts --bail=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "loads attempt history ordered by attemptNumber and preserves the latest snapshot after mixed retries" tests/storage/ingestion-session-repository.test.ts` finds the repository lifecycle regression.
    - `rg -n "incremental-extraction-workflow.test.ts" package.json` confirms the standard ingestion script includes the new lifecycle file.
    - `rg -n "attemptNumber" tests/storage/ingestion-session-repository.test.ts` finds the ordered-attempt assertions.
    - `npm exec -- vitest run tests/storage/ingestion-session-repository.test.ts tests/services/incremental-extraction-workflow.test.ts --bail=1` passes.
  </acceptance_criteria>
  <done>The repository keeps ordered attempt lineage intact, and the normal ingestion suite now exercises the new incremental lifecycle file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 10-03-03: Add public multi-segment API lifecycle proof and run the full ingestion gate</name>
  <files>tests/api/ingestion-review-api.test.ts, tests/api/check-controls-api.test.ts, tests/storage/ingestion-session-repository.test.ts, package.json</files>
  <read_first>
    - tests/api/ingestion-review-api.test.ts
    - tests/api/check-controls-api.test.ts
    - tests/storage/ingestion-session-repository.test.ts
    - package.json
    - src/api/routes/ingestion-extract.ts
    - src/services/ingestion-check.ts
  </read_first>
  <action>Extend `tests/api/ingestion-review-api.test.ts` with `serializes append-only attempts and progressSummary during multi-segment retry flows`, and extend `tests/api/check-controls-api.test.ts` with `passes manual check only after the last reopened segment is reapproved`. Use a three-segment API fixture where segment 1 stays approved, segment 2 fails once then succeeds, and segment 3 is reopened with `{ allowApprovalReset: true }`. Assert `progressSummary.failedSegments === 0` after the final successful retry, `segments[2].attempts.length === 2` for the reopened segment, the untouched approved segment keeps its original `approvedAt`, and the first `/check` call after reopening segment 3 returns 409 while the final `/check` after reapproval returns 200 with `workflowState: "checked"`. After those tests exist, run `npm run typecheck && npm run test:ingestion` and close any assertion gaps only in the touched lifecycle tests or `package.json` script order.</action>
  <verify>
    <automated>npm run typecheck && npm run test:ingestion</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "serializes append-only attempts and progressSummary during multi-segment retry flows" tests/api/ingestion-review-api.test.ts` finds the additive API lifecycle regression.
    - `rg -n "passes manual check only after the last reopened segment is reapproved" tests/api/check-controls-api.test.ts` finds the end-to-end manual check gate proof.
    - `npm run typecheck` passes.
    - `npm run test:ingestion` passes with `tests/services/incremental-extraction-workflow.test.ts` included in the executed script.
  </acceptance_criteria>
  <done>The full ingestion validation gate proves selected extraction, retry, partial approval, approval reset, provenance safety, and the unchanged full-session check contract together.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service/repository state -> automated regressions | The new retry and approval semantics are only trustworthy if tests exercise cross-layer state transitions. |
| package test script -> phase gate | The normal ingestion suite must actually run the new lifecycle file or the safety net is incomplete. |
| public API lifecycle -> manual /check | The public routes must prove the full-session gate survives retry and partial approval flows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | retry-reset public workflow | mitigate | Add API and service regressions that prove approved targets reopen only under explicit `allowApprovalReset` semantics. |
| T-10-02 | Repudiation | attempt lineage visibility | mitigate | Add repository and API lifecycle assertions for ordered attempts, final latest snapshot, and progress summaries. |
| T-10-05 | Tampering | full-session manual check | mitigate | Add end-to-end API tests proving `/check` stays blocked until the last reopened segment is reapproved. |
</threat_model>

<verification>
Run the full Phase 10 lifecycle gate:

```bash
npm exec -- vitest run tests/services/incremental-extraction-workflow.test.ts tests/services/ingestion-review-workflow.test.ts tests/storage/ingestion-session-repository.test.ts tests/api/ingestion-review-api.test.ts tests/api/check-controls-api.test.ts --bail=1
npm run typecheck
npm run test:ingestion
```
</verification>

<success_criteria>
- `tests/services/incremental-extraction-workflow.test.ts` proves mixed success/failure/retry/reapproval across multiple segments.
- `tests/storage/ingestion-session-repository.test.ts` proves attempt history order and latest-snapshot integrity after retries.
- `tests/api/ingestion-review-api.test.ts` and `tests/api/check-controls-api.test.ts` prove the public workflow still blocks `/check` until every segment is approved and current.
- `package.json` runs the incremental lifecycle file in the standard ingestion gate.
</success_criteria>

<output>
After completion, create `.planning/phases/10-incremental-extraction-and-review-resilience/10-03-SUMMARY.md`.
</output>
