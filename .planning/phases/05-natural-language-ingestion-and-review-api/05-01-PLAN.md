---
phase: 05-natural-language-ingestion-and-review-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/ingestion.ts
  - src/domain/index.ts
  - src/storage/migrations/0003_ingestion_review.sql
  - src/storage/schema.ts
  - src/storage/index.ts
  - src/storage/repositories/ingestion-session-repository.ts
  - src/services/ingestion-llm-client.ts
  - src/services/ingestion-session.ts
  - src/services/index.ts
  - src/api/app.ts
  - src/api/schemas.ts
  - src/api/routes/ingestion-submit.ts
  - src/api/routes/ingestion-extract.ts
  - src/api/routes/ingestion-read.ts
  - package.json
  - tests/storage/ingestion-session-repository.test.ts
  - tests/services/natural-language-extraction.test.ts
  - tests/api/ingestion-review-api.test.ts
autonomous: true
requirements: [FLOW-01]
user_setup: []
must_haves:
  truths:
    - Writers can submit either a scene chunk or a full draft and receive a persisted review session instead of an immediate canonical write.
    - Full-draft submissions are split into explicit reviewable segments before extraction output is shown.
    - Extracted candidates carry confidence, source-span provenance, and review-needed flags while remaining advisory and non-canonical.
  artifacts:
    - src/domain/ingestion.ts
    - src/storage/migrations/0003_ingestion_review.sql
    - src/storage/repositories/ingestion-session-repository.ts
    - src/services/ingestion-session.ts
    - src/api/app.ts
    - tests/services/natural-language-extraction.test.ts
    - tests/api/ingestion-review-api.test.ts
  key_links:
    - src/api/routes/ingestion-submit.ts passes validated submit payloads into src/services/ingestion-session.ts instead of creating canonical records directly.
    - src/services/ingestion-session.ts writes review sessions through src/storage/repositories/ingestion-session-repository.ts and normalizes extraction results against src/domain/entities.ts, src/domain/state.ts, src/domain/events.ts, and src/domain/rules.ts.
    - tests/api/ingestion-review-api.test.ts proves POST submit, POST extract, and GET read expose explicit workflow states and segment/candidate metadata.
---

<objective>
Build the natural-language intake foundation for Phase 5: submission contracts, deterministic draft segmentation, advisory extraction normalization, review-session persistence, and the first submit/extract/read API surface.

Purpose: Let writers work from prose-like input without bypassing the structured reasoning core or canonical persistence boundaries.
Output: Ingestion domain contracts, review-session storage, extraction orchestration, Fastify app factory, and deterministic service/API regressions.
</objective>

<execution_context>
@/home/usow/Desktop/storyenginelogic/.codex/get-shit-done/workflows/execute-plan.md
@/home/usow/Desktop/storyenginelogic/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-natural-language-ingestion-and-review-api/05-CONTEXT.md
@.planning/phases/05-natural-language-ingestion-and-review-api/05-RESEARCH.md
@.planning/phases/05-natural-language-ingestion-and-review-api/05-VALIDATION.md
@src/domain/entities.ts
@src/domain/state.ts
@src/domain/events.ts
@src/domain/rules.ts
@src/storage/schema.ts
@src/storage/repositories/story-repository.ts
@src/storage/repositories/provenance-repository.ts
@package.json
@vitest.config.ts
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define ingestion contracts and review-session persistence schema</name>
  <files>src/domain/ingestion.ts, src/domain/index.ts, src/storage/migrations/0003_ingestion_review.sql, src/storage/schema.ts, src/storage/index.ts, src/storage/repositories/ingestion-session-repository.ts</files>
  <read_first>.planning/phases/05-natural-language-ingestion-and-review-api/05-CONTEXT.md, .planning/phases/05-natural-language-ingestion-and-review-api/05-RESEARCH.md, src/domain/index.ts, src/domain/entities.ts, src/domain/state.ts, src/domain/events.ts, src/domain/rules.ts, src/storage/schema.ts</read_first>
  <action>Create `src/domain/ingestion.ts` and export it from `src/domain/index.ts`. In that file define the exact schemas `SubmissionInputKindSchema` with values `chunk` and `full_draft`, `IngestionWorkflowStateSchema` with values `submitted`, `extracted`, `needs_review`, `partially_approved`, `approved`, and `checked`, `StructuredCandidateKindSchema` with values `entity`, `state_boundary`, `event`, `causal_link`, and `rule`, and `ReviewNeededReasonSchema` with values `low_confidence`, `conflicting_candidates`, and `normalization_failed`. Also define `IngestionSessionRecordSchema`, `IngestionSegmentRecordSchema`, `IngestionCandidateRecordSchema`, `StructuredExtractionBatchSchema`, and `IngestionSessionSnapshotSchema` so Phase 5 has one typed contract for D-01, D-02, D-03, D-09, D-11, D-13, D-14, D-16, D-17, D-18, D-19, and D-20. Create `src/storage/migrations/0003_ingestion_review.sql` with the exact tables `ingestion_sessions`, `ingestion_segments`, and `ingestion_candidates`; keep them separate from canonical story and verdict tables per D-20. `ingestion_sessions` must include `session_id`, nullable `story_id` and `revision_id`, `draft_title`, `default_rule_pack_name`, `input_kind`, `raw_text`, `workflow_state`, `prompt_family`, `model_name`, nullable `last_verdict_run_id`, `created_at`, `updated_at`, and nullable `last_checked_at`. `ingestion_segments` must include `segment_id`, `session_id`, `sequence`, `label`, `start_offset`, `end_offset`, `segment_text`, `workflow_state`, and nullable `approved_at`. `ingestion_candidates` must include `candidate_id`, `session_id`, `segment_id`, `candidate_kind`, `canonical_key`, `confidence`, `review_needed`, `review_needed_reason`, `source_span_start`, `source_span_end`, `provenance_detail`, `extracted_payload`, nullable `corrected_payload`, and nullable `normalized_payload`. Update `src/storage/schema.ts` so `canonicalTableNames` includes the three new ingestion tables and update `src/storage/index.ts` to export `IngestionSessionRepository`. Implement `src/storage/repositories/ingestion-session-repository.ts` with the exact methods `createSession`, `saveSegments`, `saveExtractionBatch`, `loadSessionSnapshot`, and `setSessionState`.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/domain/ingestion.ts contains `SubmissionInputKindSchema`.
    - src/domain/ingestion.ts contains `IngestionWorkflowStateSchema`.
    - src/domain/ingestion.ts contains `needs_review`, `partially_approved`, and `checked`.
    - src/storage/migrations/0003_ingestion_review.sql contains `CREATE TABLE IF NOT EXISTS ingestion_sessions`.
    - src/storage/migrations/0003_ingestion_review.sql contains `CREATE TABLE IF NOT EXISTS ingestion_segments`.
    - src/storage/migrations/0003_ingestion_review.sql contains `CREATE TABLE IF NOT EXISTS ingestion_candidates`.
    - src/storage/repositories/ingestion-session-repository.ts exports `IngestionSessionRepository`.
    - src/storage/repositories/ingestion-session-repository.ts contains `saveExtractionBatch`.
  </acceptance_criteria>
  <done>Phase 5 has explicit ingestion-state contracts and a dedicated persistence boundary for review sessions, segments, and advisory candidates.</done>
</task>

<task type="auto">
  <name>Task 2: Implement deterministic segmentation and advisory extraction orchestration</name>
  <files>src/services/ingestion-llm-client.ts, src/services/ingestion-session.ts, src/services/index.ts</files>
  <read_first>.planning/phases/05-natural-language-ingestion-and-review-api/05-CONTEXT.md, .planning/phases/05-natural-language-ingestion-and-review-api/05-RESEARCH.md, src/domain/ingestion.ts, src/domain/entities.ts, src/domain/state.ts, src/domain/events.ts, src/domain/rules.ts, src/storage/repositories/ingestion-session-repository.ts, src/services/index.ts</read_first>
  <action>Create `src/services/ingestion-llm-client.ts` exporting `IngestionLlmClient`, `StructuredExtractionEnvelope`, and `createConfiguredIngestionLlmClient`. The client must represent the single-model Phase 5 extraction adapter, persist one configured `modelName`, and hardcode the prompt-family string `phase5-default` for D-19 without leaking provider-native payloads outside this file. Create `src/services/ingestion-session.ts` exporting `segmentSubmissionText`, `submitIngestionSession`, `extractIngestionSession`, and `getIngestionSessionSnapshot`. `submitIngestionSession` must accept either `storyId` plus `revisionId` or a new-draft payload with `draftTitle` and `defaultRulePackName` per D-03; when the caller starts a new draft session, reserve `story:draft:<sessionId>` and `revision:draft:<sessionId>` as the target IDs stored on the session record so downstream canonical promotion has a stable target. `segmentSubmissionText` must split `full_draft` input on heading markers (`Scene`, `Chapter`, or Markdown headings) and blank-line scene breaks first, then fall back to five-sentence windows capped at 1200 characters per segment so D-02 is deterministic and reviewable. Chunk submissions must become exactly one segment per D-01. `extractIngestionSession` must call the single-model client, normalize extracted entities, state boundaries, events, causal links, and rules against the existing Zod schemas in `src/domain/*.ts`, and persist extracted, corrected, and normalized payload slots through `IngestionSessionRepository.saveExtractionBatch`. Set `review_needed=true` and `reviewNeededReason=low_confidence` when confidence is below `0.75`, `reviewNeededReason=conflicting_candidates` when multiple candidates share the same `canonicalKey`, and `reviewNeededReason=normalization_failed` when Zod validation or canonical normalization fails, per D-13, D-16, D-17, and D-18. Successful extraction should move the session to `extracted`; any flagged segment should move it to `needs_review`. Do not call `StoryRepository`, `RuleRepository`, or `executeVerdictRun` in this service; submit/extract stays advisory only per D-12 and D-20. Re-export the new service functions from `src/services/index.ts`.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/services/ingestion-llm-client.ts exports `createConfiguredIngestionLlmClient`.
    - src/services/ingestion-llm-client.ts contains `phase5-default`.
    - src/services/ingestion-session.ts exports `segmentSubmissionText`.
    - src/services/ingestion-session.ts exports `submitIngestionSession`.
    - src/services/ingestion-session.ts exports `extractIngestionSession`.
    - src/services/ingestion-session.ts exports `getIngestionSessionSnapshot`.
    - src/services/ingestion-session.ts contains `story:draft:<sessionId>` or `revision:draft:<sessionId>`.
    - src/services/ingestion-session.ts contains `low_confidence`, `conflicting_candidates`, and `normalization_failed`.
  </acceptance_criteria>
  <done>Submission and extraction are orchestrated as a deterministic, advisory review-session flow instead of a direct canonical import.</done>
</task>

<task type="auto">
  <name>Task 3: Add the submit/extract/read Fastify surface and deterministic regressions</name>
  <files>src/api/app.ts, src/api/schemas.ts, src/api/routes/ingestion-submit.ts, src/api/routes/ingestion-extract.ts, src/api/routes/ingestion-read.ts, package.json, tests/storage/ingestion-session-repository.test.ts, tests/services/natural-language-extraction.test.ts, tests/api/ingestion-review-api.test.ts</files>
  <read_first>.planning/phases/05-natural-language-ingestion-and-review-api/05-VALIDATION.md, src/domain/ingestion.ts, src/services/ingestion-session.ts, src/storage/repositories/ingestion-session-repository.ts, package.json, vitest.config.ts</read_first>
  <action>Add `fastify` as the first HTTP framework dependency in `package.json`. Create `src/api/schemas.ts` with the exact request/response schemas `SubmitIngestionRequestSchema`, `ExtractSubmissionRequestSchema`, and `IngestionSessionResponseSchema`. Create `src/api/routes/ingestion-submit.ts`, `src/api/routes/ingestion-extract.ts`, and `src/api/routes/ingestion-read.ts` and register them from `src/api/app.ts` via a `buildStoryGraphApi` factory. The exact route paths must be `POST /api/ingestion/submissions`, `POST /api/ingestion/submissions/:sessionId/extract`, and `GET /api/ingestion/submissions/:sessionId`. Keep handlers thin: validate with the Zod schemas, call the functions from `src/services/ingestion-session.ts`, and return `sessionId`, `workflowState`, `storyId`, `revisionId`, `segments`, and segment-level candidate arrays with `confidence`, `reviewNeeded`, `reviewNeededReason`, `sourceSpanStart`, `sourceSpanEnd`, and `provenanceDetail`. Create `tests/storage/ingestion-session-repository.test.ts` covering session, segment, and candidate round-trips including nullable `correctedPayload` and source-span fields. Create `tests/services/natural-language-extraction.test.ts` covering chunk vs full-draft segmentation, candidate normalization, and `needs_review` routing. Create `tests/api/ingestion-review-api.test.ts` using Fastify injection so the Phase 5 API verifies `submit -> extract -> read` without opening ports. The API test must prove that submission returns `submitted`, extraction returns `extracted` or `needs_review`, and read returns persisted segment/candidate metadata without any canonical promotion.</action>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/storage/ingestion-session-repository.test.ts tests/services/natural-language-extraction.test.ts tests/api/ingestion-review-api.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"fastify"`.
    - src/api/app.ts contains `buildStoryGraphApi`.
    - src/api/routes/ingestion-submit.ts contains `/api/ingestion/submissions`.
    - src/api/routes/ingestion-extract.ts contains `/api/ingestion/submissions/:sessionId/extract`.
    - src/api/routes/ingestion-read.ts contains `/api/ingestion/submissions/:sessionId`.
    - tests/storage/ingestion-session-repository.test.ts contains `correctedPayload`.
    - tests/services/natural-language-extraction.test.ts contains `full draft` or `full_draft`.
    - tests/api/ingestion-review-api.test.ts contains `submitted`, `extracted`, and `needs_review`.
  </acceptance_criteria>
  <done>Phase 5 exposes a thin, tested submit/extract/read API that surfaces advisory review data without touching canonical state.</done>
</task>

</tasks>

<threat_model>
  <trust_boundaries>
    - Natural-language prose crosses into segmentation and extraction services and must not become canonical truth on submit or extract.
    - Advisory extraction candidates cross into durable review-session storage and must keep enough provenance to survive later approval and audit.
  </trust_boundaries>
  <threats>
    <threat>
      <threat_id>T-05-01</threat_id>
      <category>Tampering</category>
      <component>submitIngestionSession and extractIngestionSession</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Persist submit/extract output only in `ingestion_sessions`, `ingestion_segments`, and `ingestion_candidates`, and block any canonical write through `StoryRepository`, `RuleRepository`, or `executeVerdictRun` during Plan 05-01.</mitigation_plan>
    </threat>
    <threat>
      <threat_id>T-05-02</threat_id>
      <category>Repudiation</category>
      <component>ingestion_candidates provenance trail</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Persist `prompt_family`, configured `model_name`, source-span offsets, `provenance_detail`, extracted payload, normalized payload, and review-needed reason on every candidate so extraction decisions remain inspectable.</mitigation_plan>
    </threat>
  </threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] npm run typecheck passes
- [ ] npx vitest run tests/storage/ingestion-session-repository.test.ts tests/services/natural-language-extraction.test.ts tests/api/ingestion-review-api.test.ts passes
</verification>

<success_criteria>
- Chunk and full-draft submissions create persisted review sessions with explicit segment boundaries.
- Extraction returns advisory candidate structures with confidence, provenance, and `review_needed` markers instead of canonical writes.
- The Fastify submit/extract/read surface is injection-tested and exposes the explicit workflow state machine.
</success_criteria>

<output>
After completion, create .planning/phases/05-natural-language-ingestion-and-review-api/05-01-SUMMARY.md
</output>
