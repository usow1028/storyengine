---
phase: 08-interactive-inspection-surface
plan: 06
type: execute
wave: 5
depends_on: ["08-03", "08-05"]
files_modified:
  - src/api/app.ts
  - src/api/routes/inspection-ui.ts
  - playwright.config.ts
  - tests/browser/inspection-test-server.ts
  - tests/browser/inspection-surface.spec.ts
autonomous: true
requirements: [FLOW-02]
user_setup: []
must_haves:
  truths:
    - "Fastify serves the built inspection shell under `/inspection/` without shadowing `/api/inspection/*`."
    - "Playwright opens a real browser page for `/inspection/runs/:runId` backed by a seeded Fastify API."
    - "Browser assertions prove first-screen console, locked group order, selection, evidence, timeline, repairs, trace, advisory separation, and raw prior redaction."
    - "The full Phase 8 browser gate is automated through `npm run build && npm run test:browser`."
  artifacts:
    - path: "src/api/routes/inspection-ui.ts"
      provides: "Fastify static UI route under `/inspection/`"
      exports: ["registerInspectionUiRoutes"]
    - path: "tests/browser/inspection-test-server.ts"
      provides: "Seeded local Fastify server for Playwright"
    - path: "tests/browser/inspection-surface.spec.ts"
      provides: "Real browser verification for UI-SPEC hooks"
    - path: "playwright.config.ts"
      provides: "Web-server orchestration for browser gate"
  key_links:
    - from: "src/api/app.ts"
      to: "src/api/routes/inspection-ui.ts"
      via: "optional `inspectionUiDistDir` dependency"
      pattern: "registerInspectionUiRoutes"
    - from: "tests/browser/inspection-test-server.ts"
      to: "src/api/app.ts"
      via: "seeded `buildStoryGraphApi` server"
      pattern: "buildStoryGraphApi"
    - from: "tests/browser/inspection-surface.spec.ts"
      to: "/inspection/runs/:runId"
      via: "Playwright navigation"
      pattern: "page.goto"
---

<objective>
Serve the browser inspection surface through Fastify and verify it in Chromium.

Purpose: Close D-02 and D-03 with an actual browser-usable view, proving the UI-SPEC and FLOW-02 behavior at the static route plus API boundary instead of only unit/component tests.
Output: Fastify static UI route, seeded Playwright test server, browser spec, and final browser verification command.
</objective>

<execution_context>
@/home/usow/Desktop/storygraph/.codex/get-shit-done/workflows/execute-plan.md
@/home/usow/Desktop/storygraph/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-interactive-inspection-surface/08-CONTEXT.md
@.planning/phases/08-interactive-inspection-surface/08-RESEARCH.md
@.planning/phases/08-interactive-inspection-surface/08-VALIDATION.md
@.planning/phases/08-interactive-inspection-surface/08-UI-SPEC.md
@.planning/phases/08-interactive-inspection-surface/08-03-SUMMARY.md
@.planning/phases/08-interactive-inspection-surface/08-05-SUMMARY.md
@.planning/phases/07-soft-prior-runtime-integration/07-VERIFICATION.md
@.planning/phases/07-soft-prior-runtime-integration/07-SECURITY.md
@AGENTS.md

<interfaces>
Existing API/app pattern:
```typescript
export interface StoryGraphApiDependencies {
  ingestionSessionRepository: IngestionSessionRepository;
  storyRepository: StoryRepository;
  ruleRepository: RuleRepository;
  provenanceRepository: ProvenanceRepository;
  verdictRepository: VerdictRepository;
  verdictRunRepository: VerdictRunRepository;
  llmClient: IngestionLlmClient;
  softPriorConfig?: SoftPriorRuntimeConfig;
  now?: () => string;
  generateId?: () => string;
}

export function buildStoryGraphApi(dependencies: StoryGraphApiDependencies): FastifyInstance;
```

Browser verification hooks from UI-SPEC:
- `Inspection Console` and `Verdict Triage` visible without landing content.
- Headings appear in order: `Hard Contradiction`, `Repairable Gap`, `Soft Drift`, `Consistent`.
- Selection changes detail while triage remains visible.
- `Evidence Summary`, `Event Timeline`, `Repair Candidates`, and `Structured Trace` are reachable.
- `Deterministic Verdict` and `Advisory Pattern Signal` are separate.
- `Pattern signal only. Hard verdict remains deterministic.` is visible.
- Browser payload/rendered text do not include `sourceWorkIds`, `snapshotDir`, or `snapshotSet`.
</interfaces>

Decision coverage:
- D-02: browser verification is automated.
- D-03: the final proof is a browser-usable Fastify-served view, not JSON only.
- D-04 through D-16: Playwright asserts the user-visible locked UI behaviors and hard/soft boundary.

Manual recovery note: if `npm run test:browser` reports missing browsers, run `npx playwright install chromium` and rerun `npm run test:browser`. This is environment setup recovery only; product behavior still requires automated browser tests.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Serve the built inspection shell through Fastify</name>
  <files>src/api/app.ts, src/api/routes/inspection-ui.ts, playwright.config.ts, tests/browser/inspection-test-server.ts</files>
  <read_first>.planning/phases/08-interactive-inspection-surface/08-RESEARCH.md, .planning/phases/08-interactive-inspection-surface/08-VALIDATION.md, .planning/phases/08-interactive-inspection-surface/08-03-SUMMARY.md, .planning/phases/08-interactive-inspection-surface/08-05-SUMMARY.md, src/api/app.ts, src/api/routes/inspection.ts, src/domain/inspection.ts, src/services/verdict-runner.ts, tests/api/inspection-api.test.ts, tests/api/check-controls-api.test.ts, tests/fixtures/hard-constraint-fixtures.ts, tests/fixtures/soft-prior-fixtures.ts, playwright.config.ts</read_first>
  <behavior>
    - Test 1: `GET /inspection/runs/:runId` returns the built `index.html` shell.
    - Test 2: Static assets are served under `/inspection/assets/` and not under `/api/`.
    - Test 3: `GET /api/inspection/runs/:runId` still returns JSON after the UI route is registered.
    - Test 4: The Playwright test server seeds a real run through existing repositories and `executeVerdictRun`, not a mock component import.
  </behavior>
  <action>Create `src/api/routes/inspection-ui.ts` with `registerInspectionUiRoutes(app, { uiDistDir })`. Use `@fastify/static` for built Vite assets under `/inspection/assets/`. Return `dist/ui/index.html` for `GET /inspection` and `GET /inspection/runs/:runId`; `/inspection` allows the approved empty state, while `/inspection/runs/:runId` allows run inspection. Do not register any static route under `/api/` and do not shadow `/api/inspection/runs/:runId`. Extend `StoryGraphApiDependencies` in `src/api/app.ts` with optional `inspectionUiDistDir?: string` and register UI routes only when that directory is provided. Update `playwright.config.ts` only if needed to keep `webServer.command` as `npm run build && npm run inspection:test-server`, port `4178`, and Chromium project. Create `tests/browser/inspection-test-server.ts` that builds a pg-mem database, applies the canonical schema, seeds or persists the hard-constraint fixture data through existing repositories, executes `executeVerdictRun` with deterministic `createdAt`, boundary facts, and a server-side available `snapshotSet` from the soft-prior fixtures, then starts `buildStoryGraphApi` on `127.0.0.1:4178` with `inspectionUiDistDir: "dist/ui"`. Print the seeded run ID for debugging but keep tests using a deterministic known ID or endpoint helper.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/api/routes/inspection-ui.ts` exists.
    - `src/api/routes/inspection-ui.ts` contains `/inspection/assets/`.
    - `src/api/routes/inspection-ui.ts` contains `/inspection/runs/:runId`.
    - `src/api/routes/inspection-ui.ts` does not contain `/api/inspection`.
    - `src/api/app.ts` contains `inspectionUiDistDir`.
    - `src/api/app.ts` contains `registerInspectionUiRoutes`.
    - `tests/browser/inspection-test-server.ts` contains `executeVerdictRun`.
    - `tests/browser/inspection-test-server.ts` contains `buildStoryGraphApi`.
    - `tests/browser/inspection-test-server.ts` contains `buildSoftPriorArtifactsFixture` or the current soft-prior fixture helper that creates an available `snapshotSet`.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>The Fastify app can serve the built inspection UI and JSON API side by side without route shadowing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Playwright browser verification for the inspection surface</name>
  <files>tests/browser/inspection-surface.spec.ts</files>
  <read_first>.planning/phases/08-interactive-inspection-surface/08-UI-SPEC.md, .planning/phases/08-interactive-inspection-surface/08-VALIDATION.md, tests/browser/inspection-test-server.ts, playwright.config.ts, src/ui/components/InspectionShell.tsx, src/ui/components/VerdictTriageList.tsx, src/ui/components/VerdictDetailPanel.tsx, src/ui/components/EvidenceTimeline.tsx, src/ui/components/TraceFields.tsx, src/ui/components/SoftPriorAdvisoryBand.tsx</read_first>
  <behavior>
    - Test 1: Visiting `/inspection/runs/:runId` shows `Inspection Console` and `Verdict Triage` without landing-page copy.
    - Test 2: Group headings appear in order: `Hard Contradiction`, `Repairable Gap`, `Soft Drift`, `Consistent`.
    - Test 3: Clicking a verdict changes selected detail while triage remains visible.
    - Test 4: `Deterministic Verdict`, `Evidence Summary`, `Event Timeline`, `Repair Candidates`, `Structured Trace`, and `Advisory Pattern Signal` are visible or reachable.
    - Test 5: The advisory boundary copy `Pattern signal only. Hard verdict remains deterministic.` is visible.
    - Test 6: Browser API response text and rendered page text do not include `sourceWorkIds`, `snapshotDir`, or `snapshotSet`.
    - Test 7: `/api/inspection/runs/:runId` returns JSON while `/inspection/runs/:runId` returns HTML.
  </behavior>
  <action>Create `tests/browser/inspection-surface.spec.ts` using `@playwright/test`. Navigate to the seeded run route from Task 1, assert the UI-SPEC verification hooks, click the first available verdict row with role-based locators, expand structured trace, and assert advisory boundary copy. Fetch `/api/inspection/runs/:runId` through the browser context and assert JSON response content type/shape separately from the HTML shell route. Check both serialized API payload text and `page.locator("body")` text for absence of `sourceWorkIds`, `snapshotDir`, and `snapshotSet`. Keep tests role/text based; do not import React components directly or bypass the Fastify static/API boundary. If Chromium is missing, run `npx playwright install chromium` and rerun the automated command.</action>
  <verify>
    <automated>npm run build && npm run test:browser</automated>
  </verify>
  <acceptance_criteria>
    - `tests/browser/inspection-surface.spec.ts` contains `page.goto`.
    - `tests/browser/inspection-surface.spec.ts` contains `Inspection Console`.
    - `tests/browser/inspection-surface.spec.ts` contains `Verdict Triage`.
    - `tests/browser/inspection-surface.spec.ts` contains `Hard Contradiction`, `Repairable Gap`, `Soft Drift`, and `Consistent`.
    - `tests/browser/inspection-surface.spec.ts` contains `Pattern signal only. Hard verdict remains deterministic.`.
    - `tests/browser/inspection-surface.spec.ts` contains `sourceWorkIds`, `snapshotDir`, and `snapshotSet` as negative assertions.
    - `tests/browser/inspection-surface.spec.ts` does not import from `src/ui`.
    - `npm run build && npm run test:browser` exits 0.
  </acceptance_criteria>
  <done>A real browser proves the Phase 8 inspection surface is usable, traceable, hard/soft-separated, redacted, and served through Fastify.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| built UI assets -> Fastify | Files from `dist/ui` are served under `/inspection/`. |
| browser -> Fastify API | Playwright browser uses the real `/api/inspection/runs/:runId` route. |
| seeded test data -> browser proof | Fixture data crosses through storage, services, API, static serving, and React rendering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-24 | Tampering / Availability | static UI route registration | mitigate | Serve assets under `/inspection/assets/`, HTML under `/inspection` and `/inspection/runs/:runId`, and assert `/api/inspection/*` remains JSON. |
| T-08-25 | Information Disclosure | browser-visible output | mitigate | Browser tests inspect API payload text and rendered page text for absence of raw prior fields and server prior config. |
| T-08-26 | Repudiation | browser verification evidence | mitigate | Playwright asserts all user-visible traceability sections and records execution through `npm run test:browser`. |
| T-08-27 | Denial of Service | static serving root | mitigate | Register `@fastify/static` against the built `dist/ui` root only; do not expose project root or source directories. |
| T-08-28 | Spoofing | mocked browser proof risk | mitigate | Browser spec navigates the served app and fetches real API output; it must not import React components directly. |
</threat_model>

<verification>
Overall checks for this plan:
- [ ] `npm run build`
- [ ] `npm run build && npm run test:browser`
- [ ] `rg "sourceWorkIds|snapshotDir|snapshotSet" src/api src/services src/ui tests/browser` shows only negative test assertions or safe fixture setup, not browser/API exposure
- [ ] `rg "from .*src/ui" tests/browser/inspection-surface.spec.ts` returns no matches
</verification>

<success_criteria>
- Fastify serves the inspection UI under `/inspection/` and preserves `/api/inspection/*` JSON routes.
- Playwright verifies the full browser inspection path against seeded real API data.
- Locked UI-SPEC verification hooks pass in Chromium.
- Raw prior artifacts are absent from browser payload and rendered text.
- Phase 8 has an automated final browser gate.
</success_criteria>

<output>
After completion, create `.planning/phases/08-interactive-inspection-surface/08-06-SUMMARY.md`
</output>
