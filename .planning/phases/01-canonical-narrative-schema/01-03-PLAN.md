---
phase: 01-canonical-narrative-schema
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/domain/transitions.ts
  - src/services/snapshot-rebuilder.ts
  - src/services/story-boundary-query.ts
  - src/services/index.ts
  - tests/canonical/reconstruction.test.ts
autonomous: true
requirements: [MODEL-02, MODEL-03, DATA-01]
user_setup: []
must_haves:
  truths:
    - The system can reconstruct state before and after a chosen event boundary using stored revisions and event deltas.
    - Boundary queries can answer where a character is, what they know, and what they want at a selected story point.
    - Reconstruction output retains provenance links to the stored canonical records it used.
  artifacts:
    - src/domain/transitions.ts
    - src/services/snapshot-rebuilder.ts
    - src/services/story-boundary-query.ts
    - tests/canonical/reconstruction.test.ts
  key_links:
    - src/services/snapshot-rebuilder.ts consumes repositories from src/storage and domain transition definitions from src/domain/transitions.ts.
    - tests/canonical/reconstruction.test.ts proves boundary reconstruction against the stored canonical fixtures.
---

<objective>
Build the baseline reconstruction path that turns stored canonical deltas into usable story-boundary state.

Purpose: Deliver the first executable proof that StoryGraph can answer state-at-boundary questions instead of only storing disconnected records.
Output: Transition definitions, snapshot reconstruction services, boundary query helpers, and reconstruction tests.
</objective>

<execution_context>
@/home/usow/Desktop/storygraph/.codex/get-shit-done/workflows/execute-plan.md
@/home/usow/Desktop/storygraph/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canonical-narrative-schema/01-CONTEXT.md
@.planning/phases/01-canonical-narrative-schema/01-RESEARCH.md
@.planning/phases/01-canonical-narrative-schema/01-VALIDATION.md
@.planning/phases/01-canonical-narrative-schema/01-01-PLAN.md
@.planning/phases/01-canonical-narrative-schema/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define transition and boundary query primitives</name>
  <files>src/domain/transitions.ts, src/services/story-boundary-query.ts</files>
  <read_first>.planning/phases/01-canonical-narrative-schema/01-CONTEXT.md, .planning/phases/01-canonical-narrative-schema/01-RESEARCH.md, src/domain/index.ts, src/storage/index.ts</read_first>
  <action>Create an explicit transition model for state changes that occur at event boundaries. Define before-event and after-event boundary semantics, ordered event application rules, and the query input needed to ask for a character’s location, knowledge, goals, loyalties, resources, and conditions at a chosen story point. Keep the API deterministic and revision-aware: boundary queries must specify story ID, revision ID, target event or boundary marker, and whether the caller wants the state before or after the event.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - Transition primitives explicitly distinguish before-event and after-event state.
    - Boundary query inputs include story revision context and target boundary selection.
    - The query layer names the core slots required by MODEL-02 instead of hiding them in generic maps.
  </acceptance_criteria>
  <done>The project has deterministic primitives for reconstructing and querying story-boundary state.</done>
</task>

<task type="auto">
  <name>Task 2: Implement snapshot rebuilding from stored deltas and revisions</name>
  <files>src/services/snapshot-rebuilder.ts, src/services/index.ts</files>
  <read_first>.planning/phases/01-canonical-narrative-schema/01-02-PLAN.md, src/storage/index.ts, src/domain/transitions.ts</read_first>
  <action>Implement a snapshot rebuilder that loads the canonical story revision, orders relevant events, applies state deltas, and returns a reconstructed boundary snapshot. The service must preserve provenance references for the state records it used and expose a deterministic result for both before and after an event. Do not infer missing state from prose or heuristics in this phase; only use stored canonical objects, event effects, and state boundary records.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - The snapshot rebuilder accepts stored revisions and event sequences as input.
    - The result includes reconstructed core slots plus provenance references.
    - The implementation never fabricates state that is absent from canonical storage.
  </acceptance_criteria>
  <done>Stored canonical deltas can be replayed into explicit story-boundary snapshots.</done>
</task>

<task type="auto">
  <name>Task 3: Verify reconstruction against boundary scenarios</name>
  <files>tests/canonical/reconstruction.test.ts</files>
  <read_first>.planning/phases/01-canonical-narrative-schema/01-VALIDATION.md, src/services/index.ts, src/storage/index.ts</read_first>
  <action>Create reconstruction tests using canonical fixtures that prove the system can answer boundary questions. Include at least one scenario where a character changes location, knowledge, or goals across an event boundary, and assert that before-event and after-event queries return different but deterministic snapshots. Include a provenance assertion proving the reconstructed answer can cite the event or state record that caused the change.</action>
  <verify>npx vitest run tests/canonical/reconstruction.test.ts && npx vitest run</verify>
  <acceptance_criteria>
    - tests/canonical/reconstruction.test.ts checks both before-event and after-event snapshots.
    - At least one assertion verifies location and one verifies knowledge or goals.
    - The test fails if provenance is omitted from the reconstructed result.
  </acceptance_criteria>
  <done>The project can answer “where are they, what do they know, what do they want?” at a selected boundary using stored canonical data.</done>
</task>

</tasks>

<threat_model>
  <trust_boundaries>
    - Stored canonical records cross into deterministic reconstruction logic and become user-visible state answers.
    - Future rule engines will rely on boundary snapshots as evidence for verdicts.
  </trust_boundaries>
  <threats>
    <threat>
      <threat_id>T-01-06</threat_id>
      <category>Tampering</category>
      <component>State reconstruction pipeline</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Rebuild snapshots only from stored canonical revisions, state boundaries, and event deltas so boundary answers cannot drift from the operational source of truth.</mitigation_plan>
    </threat>
    <threat>
      <threat_id>T-01-07</threat_id>
      <category>Information Disclosure</category>
      <component>Boundary query results</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Return provenance-aware state answers tied to the selected revision and boundary so future consumers do not confuse data from unrelated revisions or events.</mitigation_plan>
    </threat>
  </threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] npm run typecheck passes
- [ ] npx vitest run tests/canonical/reconstruction.test.ts passes
- [ ] npx vitest run passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Boundary reconstruction can answer location, knowledge, and goals from stored canonical data
- Reconstruction results preserve provenance and distinguish before-event from after-event state
</success_criteria>

<output>
After completion, create .planning/phases/01-canonical-narrative-schema/01-03-SUMMARY.md
</output>
