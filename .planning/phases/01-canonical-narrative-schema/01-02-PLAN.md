---
phase: 01-canonical-narrative-schema
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - package.json
  - src/storage/db.ts
  - src/storage/schema.ts
  - src/storage/migrations/0001_canonical_core.sql
  - src/storage/repositories/story-repository.ts
  - src/storage/repositories/rule-repository.ts
  - src/storage/repositories/verdict-repository.ts
  - src/storage/repositories/provenance-repository.ts
  - src/storage/index.ts
  - tests/storage/persistence.test.ts
autonomous: true
requirements: [DATA-01, MODEL-02, MODEL-03, MODEL-04]
user_setup: []
must_haves:
  truths:
    - Canonical story objects, rule metadata, verdict records, and provenance are stored in a queryable relational model rather than prose blobs.
    - Story revisions and rule versions persist lineage and provenance instead of mutating in place.
    - Stable relational columns hold reasoning-critical fields, with bounded jsonb only for typed extensions and annotations.
  artifacts:
    - src/storage/schema.ts
    - src/storage/migrations/0001_canonical_core.sql
    - src/storage/repositories/story-repository.ts
    - src/storage/repositories/rule-repository.ts
    - src/storage/repositories/verdict-repository.ts
    - src/storage/repositories/provenance-repository.ts
    - tests/storage/persistence.test.ts
  key_links:
    - src/storage/schema.ts matches the canonical domain types exported by src/domain/index.ts.
    - Repository modules round-trip revision lineage and provenance through the relational schema.
---

<objective>
Turn the canonical domain contract into an operational storage model with revision lineage and provenance.

Purpose: Make the StoryGraph core queryable and replayable so later rule engines can inspect exact stored facts instead of re-parsing prose.
Output: PostgreSQL-centered schema definitions, migration artifacts, repository interfaces, and persistence tests.
</objective>

<execution_context>
@/home/usow/Desktop/storygraph/.codex/get-shit-done/workflows/execute-plan.md
@/home/usow/Desktop/storygraph/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canonical-narrative-schema/01-CONTEXT.md
@.planning/phases/01-canonical-narrative-schema/01-RESEARCH.md
@.planning/phases/01-canonical-narrative-schema/01-VALIDATION.md
@.planning/phases/01-canonical-narrative-schema/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the relational canonical storage schema</name>
  <files>package.json, src/storage/db.ts, src/storage/schema.ts, src/storage/migrations/0001_canonical_core.sql</files>
  <read_first>.planning/phases/01-canonical-narrative-schema/01-CONTEXT.md, .planning/phases/01-canonical-narrative-schema/01-RESEARCH.md, .planning/phases/01-canonical-narrative-schema/01-01-PLAN.md, src/domain/index.ts</read_first>
  <action>Add the PostgreSQL storage foundation using a thin TypeScript query layer and explicit SQL migration files. In schema.ts and the initial migration, create tables for stories, story_revisions, entities, place hierarchy, character_state_boundaries, state_extensions, events, event_preconditions, event_effects, causal_links, rule_packs, rule_versions, verdicts, and provenance records. Keep reasoning-critical identifiers and foreign keys relational. Use jsonb only for typed extension payloads, annotations, or normalized attribute values that are intentionally extensible.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - The initial migration includes tables for story revisions, rule versions, verdicts, and provenance.
    - schema.ts exposes a typed representation of the same tables and key fields.
    - No reasoning-critical field such as story IDs, revision IDs, entity IDs, event IDs, or rule IDs is stored only inside jsonb.
  </acceptance_criteria>
  <done>The project has an explicit relational source of truth for canonical story data and provenance.</done>
</task>

<task type="auto">
  <name>Task 2: Implement repository boundaries for stories, rules, verdicts, and provenance</name>
  <files>src/storage/repositories/story-repository.ts, src/storage/repositories/rule-repository.ts, src/storage/repositories/verdict-repository.ts, src/storage/repositories/provenance-repository.ts, src/storage/index.ts</files>
  <read_first>.planning/phases/01-canonical-narrative-schema/01-RESEARCH.md, src/domain/index.ts, src/storage/schema.ts</read_first>
  <action>Implement repository functions that persist and reload the canonical domain objects without silent field loss. Story repository must save stories, revisions, entities, state boundaries, events, and causal links. Rule repository must save rule metadata separately from normalized executable payload references. Verdict repository must store verdict kind, referenced entities/events/rules, and revision context. Provenance repository must attach origin metadata to entities, states, events, and rules so later explanations can trace their source.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - Story repository supports revision lineage rather than mutable overwrite semantics.
    - Rule repository keeps user-authored rule text distinct from executable rule references.
    - Verdict repository stores pointers to the canonical items used in a judgment.
    - Provenance repository can link a stored record back to its source descriptor.
  </acceptance_criteria>
  <done>Storage repositories expose a concrete persistence boundary for the canonical engine core.</done>
</task>

<task type="auto">
  <name>Task 3: Add persistence round-trip coverage for canonical fixtures</name>
  <files>tests/storage/persistence.test.ts</files>
  <read_first>.planning/phases/01-canonical-narrative-schema/01-VALIDATION.md, src/storage/index.ts, src/domain/index.ts</read_first>
  <action>Create integration-style tests that seed a minimal canonical story with one character, one abstract event, one world rule, one verdict stub, and provenance references. Assert that the repositories round-trip the story revision graph, state boundaries, rule metadata, and verdict linkages without dropping core fields. Include a failure case that would surface if a required canonical field is omitted or hidden in an untyped payload.</action>
  <verify>npx vitest run tests/storage/persistence.test.ts</verify>
  <acceptance_criteria>
    - Tests cover story revision lineage, rule version lineage, and provenance round-trips.
    - The fixture includes at least one explicit character state boundary and one causal event.
    - A missing required field causes a failing test rather than silent coercion.
  </acceptance_criteria>
  <done>Storage correctness for the canonical model is proven by executable round-trip tests.</done>
</task>

</tasks>

<threat_model>
  <trust_boundaries>
    - Canonical domain objects cross into the database write path and must retain identity, lineage, and provenance.
    - Future rule engines will trust the storage layer as the operational source of truth.
  </trust_boundaries>
  <threats>
    <threat>
      <threat_id>T-01-03</threat_id>
      <category>Tampering</category>
      <component>Canonical persistence schema</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Use explicit migrations, foreign keys, and typed repositories so canonical links cannot be silently rewritten or dropped during persistence.</mitigation_plan>
    </threat>
    <threat>
      <threat_id>T-01-04</threat_id>
      <category>Repudiation</category>
      <component>Provenance trail</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Persist provenance records and revision lineage together with stored canonical objects so future verdicts can attribute every fact to a source and revision context.</mitigation_plan>
    </threat>
    <threat>
      <threat_id>T-01-05</threat_id>
      <category>Denial of Service</category>
      <component>jsonb extension payloads</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Confine flexible payloads to bounded extension tables and keep routine query axes relational so storage queries remain predictable as stories grow.</mitigation_plan>
    </threat>
  </threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] npm run typecheck passes
- [ ] npx vitest run tests/storage/persistence.test.ts passes
- [ ] The initial migration and schema module agree on the canonical tables and foreign-key relationships
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Canonical stories, rules, verdicts, and provenance are queryable in the operational storage layer
- Revision lineage and source provenance persist without silent field loss
</success_criteria>

<output>
After completion, create .planning/phases/01-canonical-narrative-schema/01-02-SUMMARY.md
</output>
