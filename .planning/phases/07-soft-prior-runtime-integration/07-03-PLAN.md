---
phase: 07-soft-prior-runtime-integration
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - tests/fixtures/soft-prior-ingestion-fixtures.ts
  - tests/api/check-controls-api.test.ts
autonomous: true
requirements: [SOFT-01]
user_setup: []
must_haves:
  truths:
    - "The live API flow `submit -> extract -> review -> approve -> check` returns `softPrior.status === \"available\"` when server-configured fixture priors are present."
    - "The returned advisory block includes drift scores, thresholds, triggered drifts, dominant prior layer, representative pattern summary, contribution evidence, reranked repairs, and repair plausibility adjustments."
    - "The same checked story path keeps hard verdict classification, finding IDs, evidence fields, and run metadata stable whether soft priors are enabled or disabled."
    - "Phase 7 verification relies on executed service/API regressions, not static `rg` wiring checks."
  artifacts:
    - path: "tests/fixtures/soft-prior-ingestion-fixtures.ts"
      provides: "API extraction candidates that promote two canonical events matching deterministic soft-prior fixture transitions"
    - path: "tests/api/check-controls-api.test.ts"
      provides: "Cross-phase E2E regression for submit/extract/review/approve/check with available soft-prior output"
  key_links:
    - from: "tests/api/check-controls-api.test.ts"
      to: "src/api/routes/ingestion-check.ts"
      via: "Fastify `app.inject()` POST `/api/ingestion/submissions/:sessionId/check`"
      pattern: "softPrior.status"
    - from: "tests/api/check-controls-api.test.ts"
      to: "tests/fixtures/soft-prior-fixtures.ts"
      via: "injected `snapshotSet` from `buildSoftPriorArtifactsFixture`"
      pattern: "buildSoftPriorArtifactsFixture"
    - from: "tests/api/check-controls-api.test.ts"
      to: "src/storage/repositories/verdict-repository.ts"
      via: "persisted hard verdict comparison with priors enabled vs disabled"
      pattern: "listForRun"
---

<objective>
Add cross-phase API E2E regressions proving soft-prior-informed checked output works through the real ingestion flow.

Purpose: Satisfy the primary Phase 7 proof requirement by executing the same user-facing submit, extract, review, approve, and check route sequence that Phase 5 introduced, now with server-configured advisory priors.
Output: Deterministic API fixture helpers and an end-to-end regression that proves advisory availability and hard-verdict invariance.
</objective>

<execution_context>
@/home/usow/Desktop/storygraph/.codex/get-shit-done/workflows/execute-plan.md
@/home/usow/Desktop/storygraph/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-v1.0-MILESTONE-AUDIT.md
@.planning/phases/07-soft-prior-runtime-integration/07-CONTEXT.md
@.planning/phases/07-soft-prior-runtime-integration/07-RESEARCH.md
@.planning/phases/07-soft-prior-runtime-integration/07-VALIDATION.md
@.planning/phases/07-soft-prior-runtime-integration/07-01-SUMMARY.md
@.planning/phases/07-soft-prior-runtime-integration/07-02-SUMMARY.md
@.planning/phases/05-natural-language-ingestion-and-review-api/05-VERIFICATION.md
@tests/api/check-controls-api.test.ts
@tests/fixtures/soft-prior-fixtures.ts
@AGENTS.md

<interfaces>
Final API contract expected after Plans 01-02:

```typescript
// src/api/schemas.ts
export const CheckIngestionResponseSchema = z.object({
  sessionId: z.string().min(1),
  workflowState: z.literal("checked"),
  storyId: z.string().min(1),
  revisionId: z.string().min(1),
  runId: z.string().min(1),
  previousRunId: z.string().nullable(),
  softPrior: SoftPriorAdvisoryResponseSchema
});
```

Fixture prior source:

```typescript
// tests/fixtures/soft-prior-fixtures.ts
export function buildSoftPriorArtifactsFixture(): {
  artifacts: PriorExportArtifact[];
  teleportTransition: SoftPriorTransitionInput;
  oathTransition: SoftPriorTransitionInput;
}
```
</interfaces>

Locked decisions covered here: D-04, D-06, D-07, D-08, D-09, D-10, D-11.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create API fixture helpers for a soft-prior check flow</name>
  <files>tests/fixtures/soft-prior-ingestion-fixtures.ts</files>
  <read_first>.planning/phases/07-soft-prior-runtime-integration/07-CONTEXT.md, .planning/phases/07-soft-prior-runtime-integration/07-RESEARCH.md, .planning/phases/07-soft-prior-runtime-integration/07-VALIDATION.md, tests/fixtures/soft-prior-fixtures.ts, tests/fixtures/corpus-prior-fixtures.ts, src/domain/events.ts, src/domain/entities.ts, src/domain/ingestion.ts, src/services/ingestion-session.ts, tests/api/check-controls-api.test.ts</read_first>
  <behavior>
    - Test 1: The fixture returns extraction candidates with two canonical event payloads whose event types are `spell_cast` and `instant_arrival`.
    - Test 2: The `instant_arrival` payload has `time.relation === "same-window"`, `time.durationMinutes === 0`, `time.minTravelMinutes === 480`, and `placeId === "place:castle"` so hard checks can produce an impossible-travel verdict.
    - Test 3: The two event payloads contain state/rule changes that let runtime transition tokens include `resources:remove:mana_reserve`, `locationId:set:place:castle`, and `teleportation_enabled`.
  </behavior>
  <action>Create `tests/fixtures/soft-prior-ingestion-fixtures.ts`. Export `buildSoftPriorCheckCandidates(sessionId: string, segmentId: string)` returning candidate objects compatible with `createConfiguredIngestionLlmClient` extraction output. The fixture must include at least two `candidateKind: "event"` candidates with valid `CanonicalEventSchema` payloads: `event:${sessionId}:spell` using `eventType: "spell_cast"`, `sequence: 0`, `placeId: "place:ruins"`, actor `character:mage`, an effect with state change `{ subjectId: "character:mage", field: "resources", operation: "remove", value: "mana_reserve" }`, and a rule change `{ ruleVersionId: "teleportation_enabled", operation: "declare" }`; and `event:${sessionId}:arrival` using `eventType: "instant_arrival"`, `sequence: 1`, `time: { relation: "same-window", durationMinutes: 0, minTravelMinutes: 480 }`, `placeId: "place:castle"`, actor `character:mage`, an effect with state change `{ subjectId: "character:mage", field: "locationId", operation: "set", value: "place:castle" }`, and a rule change `{ ruleVersionId: "teleportation_enabled", operation: "declare" }`. Include an optional valid `candidateKind: "entity"` for `character:mage` if needed by existing fixture style, but do not require new storage schema or migrations. Export helper assertions or normalization helpers only if they reduce duplication in `tests/api/check-controls-api.test.ts`.</action>
  <verify>
    <automated>npx vitest run tests/api/check-controls-api.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/soft-prior-ingestion-fixtures.ts` exists.
    - `tests/fixtures/soft-prior-ingestion-fixtures.ts` contains `buildSoftPriorCheckCandidates`.
    - `tests/fixtures/soft-prior-ingestion-fixtures.ts` contains `spell_cast`.
    - `tests/fixtures/soft-prior-ingestion-fixtures.ts` contains `instant_arrival`.
    - `tests/fixtures/soft-prior-ingestion-fixtures.ts` contains `resources`.
    - `tests/fixtures/soft-prior-ingestion-fixtures.ts` contains `locationId`.
    - `tests/fixtures/soft-prior-ingestion-fixtures.ts` contains `teleportation_enabled`.
    - `tests/fixtures/soft-prior-ingestion-fixtures.ts` contains `minTravelMinutes: 480`.
    - `npx vitest run tests/api/check-controls-api.test.ts` exits 0 after the fixture is imported or remains unused until Task 2 completes.
  </acceptance_criteria>
  <done>The API test suite has deterministic extraction candidates that can produce both a hard check result and a matching soft-prior advisory assessment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the primary submit/extract/review/approve/check soft-prior API E2E</name>
  <files>tests/api/check-controls-api.test.ts</files>
  <read_first>.planning/phases/07-soft-prior-runtime-integration/07-CONTEXT.md, .planning/phases/07-soft-prior-runtime-integration/07-RESEARCH.md, .planning/phases/07-soft-prior-runtime-integration/07-VALIDATION.md, tests/api/check-controls-api.test.ts, tests/fixtures/soft-prior-ingestion-fixtures.ts, tests/fixtures/soft-prior-fixtures.ts, src/api/app.ts, src/api/routes/ingestion-submit.ts, src/api/routes/ingestion-extract.ts, src/api/routes/ingestion-review.ts, src/api/routes/ingestion-check.ts, src/api/schemas.ts, src/storage/repositories/verdict-repository.ts, src/storage/repositories/verdict-run-repository.ts</read_first>
  <behavior>
    - Test 1: The route sequence `POST /api/ingestion/submissions`, `POST /extract`, `PATCH /segments/:segmentId`, `POST /segments/:segmentId/approve`, and `POST /check` returns HTTP status codes `201`, `200`, `200`, `200`, and `200`.
    - Test 2: With `softPriorConfig: { enabled: true, snapshotSet, genreWeights: [{ genreKey: "fantasy", weight: 0.8 }, { genreKey: "adventure", weight: 0.2 }], worldProfile: "fantasy-light" }`, the check JSON has `softPrior.status === "available"`.
    - Test 3: The check JSON keeps top-level `sessionId`, `workflowState`, `storyId`, `revisionId`, `runId`, and `previousRunId` fields.
    - Test 4: The advisory block contains `assessment.driftScores.transition_drift`, `assessment.thresholds.transition_drift`, `assessment.triggeredDrifts`, `assessment.dominantPriorLayer`, `assessment.representativePatternSummary`, `assessment.contributions`, `rerankedRepairs`, and `repairPlausibilityAdjustments`.
    - Test 5: Running the same flow in isolated pg-mem apps with soft priors disabled and enabled yields matching persisted hard verdict classifications, finding IDs, evidence reason codes/event IDs, `triggerKind`, `createdAt`, and `previousRunId`.
  </behavior>
  <action>Extend `tests/api/check-controls-api.test.ts` with a new primary E2E test named `returns available soft-prior advisory output without mutating hard verdicts through submit extract review approve check`. Create a local helper that builds a fresh pg-mem API harness with real repositories, deterministic `generateId`, deterministic `now`, and an `llmClient` from `createConfiguredIngestionLlmClient` returning `buildSoftPriorCheckCandidates(sessionId, segmentId)`. Build `snapshotSet` from `buildSoftPriorArtifactsFixture()` by writing artifacts through existing loader-compatible helpers or by constructing the in-memory `PriorSnapshotSet` used by existing prior tests; use `snapshotSet` injection rather than a global `data/prior-snapshots` directory per D-04. Run the full route flow with a review `PATCH` that changes the segment boundary label to `Soft Prior Scene` before approval so D-09's review step is executed. Assert the exact `softPrior.status === "available"` string, assert the assessment fields required by D-07, assert `softPrior.repairPlausibilityAdjustments.length > 0`, and assert `softPrior.rerankedRepairs.length > 0`. Run the same helper once with `softPriorConfig: { enabled: false }` and once with the enabled snapshot config; compare persisted verdict rows from `verdictRepository.listForRun(runId)` and run records from `verdictRunRepository.listRunsForRevision(storyId, revisionId)` after normalizing only volatile `runId`/`verdictId` strings if necessary. The comparison must prove hard `verdictKind`, `category`, `evidence.findingId`, `evidence.reasonCode`, `evidence.eventIds`, `triggerKind`, `createdAt`, and `previousRunId` are unchanged. Do not use static `rg` checks as the completion proof.</action>
  <verify>
    <automated>npm run typecheck && npm run test:priors && npm run test:ingestion</automated>
  </verify>
  <acceptance_criteria>
    - `tests/api/check-controls-api.test.ts` contains `returns available soft-prior advisory output without mutating hard verdicts through submit extract review approve check`.
    - `tests/api/check-controls-api.test.ts` contains `softPrior.status`.
    - `tests/api/check-controls-api.test.ts` contains `"available"`.
    - `tests/api/check-controls-api.test.ts` contains `buildSoftPriorArtifactsFixture`.
    - `tests/api/check-controls-api.test.ts` contains `buildSoftPriorCheckCandidates`.
    - `tests/api/check-controls-api.test.ts` contains `repairPlausibilityAdjustments`.
    - `tests/api/check-controls-api.test.ts` contains `rerankedRepairs`.
    - `tests/api/check-controls-api.test.ts` contains `dominantPriorLayer`.
    - `tests/api/check-controls-api.test.ts` contains `representativePatternSummary`.
    - `tests/api/check-controls-api.test.ts` compares enabled and disabled persisted hard verdict output for invariance.
    - `npm run typecheck && npm run test:priors && npm run test:ingestion` exits 0.
  </acceptance_criteria>
  <done>The primary API E2E proves Phase 7 soft priors surface in the live checked flow and remain advisory against deterministic hard verdict data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture prior set -> API app dependencies | Test `snapshotSet` injection simulates trusted server config and must not become request-controlled behavior. |
| API response -> downstream clients | Clients must be able to distinguish advisory soft-prior output from hard verdict/run metadata. |
| regression comparison -> product claim | The phase can only close `SOFT-01` if executed tests prove hard verdict invariance, not if code search suggests wiring exists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11 | Tampering | API E2E prior configuration | mitigate | Use `buildStoryGraphApi({ softPriorConfig })` server dependencies and assert no request payload contains snapshot path/config fields. |
| T-07-12 | Spoofing / Tampering | advisory output semantics | mitigate | Assert `softPrior.status === "available"` under a separate block and compare persisted hard verdict/run data against a disabled-prior run. |
| T-07-13 | Information Disclosure | API response evidence | mitigate | Assert returned advisory evidence includes D-07 assessment/contribution summaries, not raw snapshot artifacts or `sourceWorkIds`. |
| T-07-14 | Denial of Service | runtime prior load path | mitigate | Plan 02 already covers missing snapshots; this E2E uses injected `snapshotSet` so the primary proof is deterministic and not filesystem-fragile. |
| T-07-15 | Repudiation | `SOFT-01` closure evidence | mitigate | Require `npm run typecheck && npm run test:priors && npm run test:ingestion` as the phase gate, with no static `rg`-only acceptance. |
</threat_model>

<verification>
Run before declaring this plan complete:
- [ ] `npx vitest run tests/engine/verdict-runner.test.ts tests/api/check-controls-api.test.ts`
- [ ] `npm run typecheck && npm run test:priors && npm run test:ingestion`
</verification>

<success_criteria>
- Full submit/extract/review/approve/check API flow returns `softPrior.status === "available"` with configured fixture priors
- Advisory output includes required D-07 assessment and repair plausibility fields
- Existing top-level check fields remain present and compatible
- Enabled soft priors do not mutate persisted hard verdict classifications, finding IDs, evidence snapshots, or run metadata
- `SOFT-01` has executed cross-phase proof suitable for Phase 7 verification
</success_criteria>

<output>
After completion, create `.planning/phases/07-soft-prior-runtime-integration/07-03-SUMMARY.md`
</output>
