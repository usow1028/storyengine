---
phase: 03-evidence-and-repair-reasoning
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - src/domain/verdicts.ts
  - src/storage/migrations/0002_verdict_runs.sql
  - src/storage/schema.ts
  - src/storage/index.ts
  - src/storage/repositories/verdict-run-repository.ts
  - src/storage/repositories/verdict-repository.ts
  - src/services/verdict-diff.ts
  - src/services/verdict-runner.ts
  - src/services/index.ts
  - package.json
  - tests/engine/verdict-runner.test.ts
  - tests/engine/verdict-diff.test.ts
  - tests/storage/persistence.test.ts
autonomous: true
requirements: [VERD-03]
user_setup: []
must_haves:
  truths:
    - Every evaluation execution is recorded as a distinct verdict run instead of overwriting prior revision-level results.
    - Rerun diffing uses stable finding IDs and compares only to the immediately previous run.
    - Diff output shows representative verdict changes plus supporting-finding additions, removals, and continuations.
  artifacts:
    - src/storage/migrations/0002_verdict_runs.sql
    - src/storage/repositories/verdict-run-repository.ts
    - src/services/verdict-runner.ts
    - src/services/verdict-diff.ts
    - tests/engine/verdict-runner.test.ts
    - tests/engine/verdict-diff.test.ts
  key_links:
    - src/storage/migrations/0002_verdict_runs.sql and src/storage/schema.ts agree on multi-migration loading and verdict-run persistence.
    - src/services/verdict-runner.ts persists run metadata through src/storage/repositories/verdict-run-repository.ts and verdict rows through src/storage/repositories/verdict-repository.ts.
    - src/services/verdict-diff.ts and tests/engine/verdict-diff.test.ts agree on stable finding ID semantics and previous-run-only comparison.
---

<objective>
Add run-aware verdict persistence and previous-run diffing for revised stories.

Purpose: Make verdicts inspectable across successive executions instead of only within a single current revision snapshot.
Output: Verdict-run persistence, stable finding IDs, previous-run diff services, and rerun regression coverage.
</objective>

<execution_context>
@/home/usow/Desktop/storygraph/.codex/get-shit-done/workflows/execute-plan.md
@/home/usow/Desktop/storygraph/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-evidence-and-repair-reasoning/03-CONTEXT.md
@.planning/phases/03-evidence-and-repair-reasoning/03-RESEARCH.md
@.planning/phases/03-evidence-and-repair-reasoning/03-VALIDATION.md
@.planning/phases/03-evidence-and-repair-reasoning/03-01-PLAN.md
@.planning/phases/03-evidence-and-repair-reasoning/03-02-PLAN.md
@src/domain/verdicts.ts
@src/storage/schema.ts
@src/storage/repositories/verdict-repository.ts
@src/services/explained-verdicts.ts
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add verdict-run persistence and stable finding identity contracts</name>
  <files>src/domain/verdicts.ts, src/storage/migrations/0002_verdict_runs.sql, src/storage/schema.ts, src/storage/index.ts, src/storage/repositories/verdict-run-repository.ts, src/storage/repositories/verdict-repository.ts, tests/storage/persistence.test.ts</files>
  <read_first>.planning/phases/03-evidence-and-repair-reasoning/03-CONTEXT.md, .planning/phases/03-evidence-and-repair-reasoning/03-RESEARCH.md, src/domain/verdicts.ts, src/storage/migrations/0001_canonical_core.sql, src/storage/schema.ts, src/storage/repositories/verdict-repository.ts, tests/storage/persistence.test.ts</read_first>
  <action>Create `src/storage/migrations/0002_verdict_runs.sql` introducing a `verdict_runs` table with at least `run_id`, `story_id`, `revision_id`, `previous_run_id`, `trigger_kind`, and `created_at`. Update `src/storage/schema.ts` so `applyCanonicalSchema` loads `0001_canonical_core.sql` and `0002_verdict_runs.sql` in lexical order, then export any new helpers from `src/storage/index.ts`. In `src/domain/verdicts.ts`, add a stable finding identifier field named `findingId` to the persisted verdict evidence contract and define a `VerdictRunRecordSchema`. Update `VerdictRepository` so verdict rows persist `run_id` and can list verdicts for a run, and create `src/storage/repositories/verdict-run-repository.ts` for run-level CRUD. Extend `tests/storage/persistence.test.ts` to prove run records, run-linked verdict rows, and `findingId` round-trip correctly.</action>
  <verify>npm run typecheck && npx vitest run tests/storage/persistence.test.ts</verify>
  <acceptance_criteria>
    - src/storage/migrations/0002_verdict_runs.sql contains `CREATE TABLE IF NOT EXISTS verdict_runs`.
    - src/storage/schema.ts references `0002_verdict_runs.sql`.
    - src/domain/verdicts.ts contains `findingId`.
    - src/domain/verdicts.ts contains `VerdictRunRecordSchema`.
    - src/storage/repositories/verdict-run-repository.ts exists and exports `VerdictRunRepository`.
  </acceptance_criteria>
  <done>StoryGraph can persist verdict runs and stable finding identity as first-class storage concepts.</done>
</task>

<task type="auto">
  <name>Task 2: Implement verdict-run execution and previous-run diff services</name>
  <files>src/services/verdict-runner.ts, src/services/verdict-diff.ts, src/services/index.ts</files>
  <read_first>.planning/phases/03-evidence-and-repair-reasoning/03-CONTEXT.md, .planning/phases/03-evidence-and-repair-reasoning/03-RESEARCH.md, .planning/phases/03-evidence-and-repair-reasoning/03-01-PLAN.md, src/services/explained-verdicts.ts, src/engine/hard-constraint-engine.ts, src/storage/repositories/verdict-run-repository.ts, src/storage/repositories/verdict-repository.ts</read_first>
  <action>Create `src/services/verdict-runner.ts` exporting `executeVerdictRun` that evaluates a revision, materializes explained verdicts, assigns deterministic `findingId` fingerprints from checker/reason/evidence fields, creates a new verdict run, and persists the resulting verdict rows under that run. Create `src/services/verdict-diff.ts` exporting `diffAgainstPreviousRun` that loads the immediately previous run only, compares current vs previous findings by `findingId`, and returns `representativeVerdictChanged`, `addedFindingIds`, `resolvedFindingIds`, `persistedFindingIds`, and `changedSupportingFindings`. Re-export both services from `src/services/index.ts`.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - src/services/verdict-runner.ts exports `executeVerdictRun`.
    - src/services/verdict-diff.ts exports `diffAgainstPreviousRun`.
    - src/services/verdict-runner.ts assigns `findingId`.
    - src/services/verdict-diff.ts contains `representativeVerdictChanged`.
    - src/services/verdict-diff.ts contains `changedSupportingFindings`.
  </acceptance_criteria>
  <done>Phase 3 has run-oriented orchestration and previous-run-only diff semantics instead of overwrite-only verdict storage.</done>
</task>

<task type="auto">
  <name>Task 3: Add rerun/diff regressions and a stable reasoning test command</name>
  <files>package.json, tests/engine/verdict-runner.test.ts, tests/engine/verdict-diff.test.ts</files>
  <read_first>.planning/phases/03-evidence-and-repair-reasoning/03-VALIDATION.md, package.json, src/services/verdict-runner.ts, src/services/verdict-diff.ts, tests/storage/persistence.test.ts</read_first>
  <action>Create `tests/engine/verdict-runner.test.ts` that proves each evaluation execution creates a distinct `verdict_run` even for the same revision and that the persisted verdicts link back to the new run. Create `tests/engine/verdict-diff.test.ts` that proves diffing compares only to the immediately previous run, preserves stable findings across reruns when `findingId` is unchanged, and reports representative verdict changes plus supporting finding additions/resolutions. Add a `test:reasoning` script to `package.json` that runs `tests/engine/evidence-traces.test.ts`, `tests/engine/repair-generator.test.ts`, `tests/engine/verdict-runner.test.ts`, and `tests/engine/verdict-diff.test.ts` without watch mode.</action>
  <verify>npm run typecheck && npx vitest run tests/engine/verdict-runner.test.ts tests/engine/verdict-diff.test.ts</verify>
  <acceptance_criteria>
    - tests/engine/verdict-runner.test.ts contains `distinct verdict run`.
    - tests/engine/verdict-diff.test.ts contains `immediately previous run`.
    - tests/engine/verdict-diff.test.ts asserts `representativeVerdictChanged`.
    - package.json contains `test:reasoning`.
    - `test:reasoning` does not use watch mode.
  </acceptance_criteria>
  <done>Verdict reruns and previous-run diffs are executable and protected by deterministic regressions.</done>
</task>

</tasks>

<threat_model>
  <trust_boundaries>
    - Verdict history crosses from transient engine output into durable run-linked audit records.
    - Stable finding IDs become the basis of rerun diff semantics and therefore must not drift with wording-only changes.
  </trust_boundaries>
  <threats>
    <threat>
      <threat_id>T-03-05</threat_id>
      <category>Repudiation</category>
      <component>Verdict run history</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Create an explicit `verdict_runs` store and link verdict rows to runs so later executions cannot overwrite or erase prior audit records.</mitigation_plan>
    </threat>
    <threat>
      <threat_id>T-03-06</threat_id>
      <category>Tampering</category>
      <component>Rerun diff identity</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Base diff continuity on deterministic `findingId` fingerprints derived from checker, reason code, and evidence fields rather than mutable explanation text.</mitigation_plan>
    </threat>
  </threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] npm run typecheck passes
- [ ] npx vitest run tests/storage/persistence.test.ts passes
- [ ] npx vitest run tests/engine/verdict-runner.test.ts tests/engine/verdict-diff.test.ts passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Every evaluation execution becomes a preserved verdict run
- Previous-run diffing is deterministic and based on stable finding IDs
</success_criteria>

<output>
After completion, create .planning/phases/03-evidence-and-repair-reasoning/03-03-SUMMARY.md
</output>
