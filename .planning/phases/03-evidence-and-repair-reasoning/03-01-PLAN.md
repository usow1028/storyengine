---
phase: 03-evidence-and-repair-reasoning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/verdicts.ts
  - src/domain/index.ts
  - src/engine/types.ts
  - src/engine/evidence-snapshots.ts
  - src/engine/explanation-renderer.ts
  - src/engine/index.ts
  - src/services/explained-verdicts.ts
  - src/services/index.ts
  - src/storage/repositories/verdict-repository.ts
  - tests/engine/evidence-traces.test.ts
  - tests/storage/persistence.test.ts
autonomous: true
requirements: [VERD-02]
user_setup: []
must_haves:
  truths:
    - Every failing verdict can be materialized into a structured evidence snapshot that cites the exact events, state boundaries, and rule versions involved.
    - Evidence snapshots include the directly relevant state axes plus the immediately preceding state change so explanation output stays locally grounded.
    - User-facing explanation text is rendered deterministically from structured evidence, not from freeform model phrasing.
  artifacts:
    - src/domain/verdicts.ts
    - src/engine/evidence-snapshots.ts
    - src/engine/explanation-renderer.ts
    - src/services/explained-verdicts.ts
    - tests/engine/evidence-traces.test.ts
  key_links:
    - src/domain/verdicts.ts and src/storage/repositories/verdict-repository.ts preserve the same structured evidence payload.
    - src/engine/evidence-snapshots.ts and src/engine/explanation-renderer.ts agree on the fields required to render deterministic explanations.
    - tests/engine/evidence-traces.test.ts proves evidence snapshots and blocked-check visibility against real engine findings.
---

<objective>
Implement the structured evidence and explanation layer that turns Phase 2 findings into explainable verdict records.

Purpose: Give StoryGraph a deterministic evidence snapshot and explanation contract before repair generation or rerun diffing is added.
Output: Richer verdict schemas, evidence snapshot builders, deterministic explanation rendering, and evidence-focused regression coverage.
</objective>

<execution_context>
@/home/usow/Desktop/storygraph/.codex/get-shit-done/workflows/execute-plan.md
@/home/usow/Desktop/storygraph/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-evidence-and-repair-reasoning/03-CONTEXT.md
@.planning/phases/03-evidence-and-repair-reasoning/03-RESEARCH.md
@.planning/phases/03-evidence-and-repair-reasoning/03-VALIDATION.md
@.planning/phases/02-hard-constraint-engine/02-02-SUMMARY.md
@.planning/phases/02-hard-constraint-engine/02-03-SUMMARY.md
@src/domain/verdicts.ts
@src/engine/types.ts
@src/engine/hard-constraint-engine.ts
@src/engine/verdict-aggregator.ts
@src/storage/repositories/verdict-repository.ts
@tests/engine/hard-constraint-engine.test.ts
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend verdict schemas for structured evidence snapshots</name>
  <files>src/domain/verdicts.ts, src/domain/index.ts, src/storage/repositories/verdict-repository.ts, tests/storage/persistence.test.ts</files>
  <read_first>.planning/phases/03-evidence-and-repair-reasoning/03-CONTEXT.md, .planning/phases/03-evidence-and-repair-reasoning/03-RESEARCH.md, src/domain/verdicts.ts, src/domain/index.ts, src/storage/repositories/verdict-repository.ts, tests/storage/persistence.test.ts</read_first>
  <action>In `src/domain/verdicts.ts`, add structured evidence snapshot contracts named `EventEvidenceSummarySchema`, `StateEvidenceSummarySchema`, `RuleEvidenceSummarySchema`, and `MissingPremiseSchema`. Extend `VerdictEvidenceSchema` so it contains `eventSummaries`, `stateSummaries`, `ruleSummaries`, `conflictPath`, and `missingPremises` in addition to the existing canonical ID arrays, supporting findings, and `notEvaluated` payload. Make `StateEvidenceSummarySchema` include both the current relevant field values and the immediate prior change source through fields named `previousBoundaryId` and `previousSourceEventId`. Re-export the new schemas from `src/domain/index.ts` and update `VerdictRepository` plus `tests/storage/persistence.test.ts` so the richer evidence payload round-trips unchanged through JSONB persistence.</action>
  <verify>npm run typecheck && npx vitest run tests/storage/persistence.test.ts</verify>
  <acceptance_criteria>
    - src/domain/verdicts.ts contains `EventEvidenceSummarySchema`.
    - src/domain/verdicts.ts contains `StateEvidenceSummarySchema`.
    - src/domain/verdicts.ts contains `previousBoundaryId`.
    - src/domain/verdicts.ts contains `previousSourceEventId`.
    - src/domain/verdicts.ts contains `conflictPath` and `missingPremises`.
    - tests/storage/persistence.test.ts asserts structured evidence summary fields survive repository round-trip.
  </acceptance_criteria>
  <done>Verdict storage can represent the exact structured evidence Phase 3 explanations require.</done>
</task>

<task type="auto">
  <name>Task 2: Build deterministic evidence snapshot and explanation helpers</name>
  <files>src/engine/types.ts, src/engine/evidence-snapshots.ts, src/engine/explanation-renderer.ts, src/engine/index.ts</files>
  <read_first>.planning/phases/03-evidence-and-repair-reasoning/03-CONTEXT.md, .planning/phases/03-evidence-and-repair-reasoning/03-RESEARCH.md, src/engine/types.ts, src/engine/hard-constraint-engine.ts, src/services/story-boundary-query.ts, src/services/snapshot-rebuilder.ts, src/domain/verdicts.ts</read_first>
  <action>Create `src/engine/evidence-snapshots.ts` exporting a function named `buildEvidenceSnapshot` that takes the representative finding, supporting findings, current event context, active rules, and character boundary facts and produces the structured evidence payload defined in `src/domain/verdicts.ts`. Create `src/engine/explanation-renderer.ts` exporting `renderDeterministicExplanation`, and build the explanation from the exact fields `reasonCode`, `eventSummaries`, `stateSummaries`, `ruleSummaries`, `conflictPath`, `missingPremises`, and `notEvaluated`. Update `src/engine/types.ts` with any explicit helper contracts needed for evidence assembly, and export the new helpers from `src/engine/index.ts`.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - src/engine/evidence-snapshots.ts exports `buildEvidenceSnapshot`.
    - src/engine/explanation-renderer.ts exports `renderDeterministicExplanation`.
    - src/engine/explanation-renderer.ts references `conflictPath`.
    - src/engine/explanation-renderer.ts references `missingPremises`.
  </acceptance_criteria>
  <done>Phase 3 has deterministic helpers for materializing evidence and rendering explanation text.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate explained verdict materialization and evidence regressions</name>
  <files>src/services/explained-verdicts.ts, src/services/index.ts, tests/engine/evidence-traces.test.ts</files>
  <read_first>.planning/phases/03-evidence-and-repair-reasoning/03-VALIDATION.md, src/engine/evidence-snapshots.ts, src/engine/explanation-renderer.ts, src/engine/hard-constraint-engine.ts, src/services/index.ts, tests/engine/hard-constraint-engine.test.ts</read_first>
  <action>Create `src/services/explained-verdicts.ts` with a function named `buildExplainedVerdictRecord` that converts a `HardConstraintEvaluation` plus canonical graph context into a persisted `VerdictRecord` using `buildEvidenceSnapshot` and `renderDeterministicExplanation`. Re-export the service from `src/services/index.ts`. Add `tests/engine/evidence-traces.test.ts` that covers at least these exact behaviors: impossible travel includes event and rule summaries in the evidence snapshot, betrayal-under-threat includes relevant state summaries and the immediate prior state change, and upstream blocked `causality` / `character` checks remain visible through `notEvaluated` in the rendered explanation path.</action>
  <verify>npm run typecheck && npx vitest run tests/engine/evidence-traces.test.ts</verify>
  <acceptance_criteria>
    - src/services/explained-verdicts.ts exports `buildExplainedVerdictRecord`.
    - tests/engine/evidence-traces.test.ts contains `impossible travel`.
    - tests/engine/evidence-traces.test.ts contains `betrayal under threat`.
    - tests/engine/evidence-traces.test.ts asserts `notEvaluated`.
  </acceptance_criteria>
  <done>Failing verdicts can be turned into deterministic explained records with regression coverage.</done>
</task>

</tasks>

<threat_model>
  <trust_boundaries>
    - Hard-engine findings cross into persisted evidence and explanation payloads that later phases will trust as audit records.
    - Blocked downstream analyses become visible explanation artifacts and must not disappear or misreport their cause.
  </trust_boundaries>
  <threats>
    <threat>
      <threat_id>T-03-01</threat_id>
      <category>Tampering</category>
      <component>Evidence snapshot assembly</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Build evidence snapshots only from canonical events, boundaries, and active rules so explanation payloads cannot drift from the actual evaluated inputs.</mitigation_plan>
    </threat>
    <threat>
      <threat_id>T-03-02</threat_id>
      <category>Repudiation</category>
      <component>Deterministic explanation output</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Persist structured evidence and render explanations deterministically so past verdicts remain explainable even after later reruns.</mitigation_plan>
    </threat>
  </threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] npm run typecheck passes
- [ ] npx vitest run tests/storage/persistence.test.ts passes
- [ ] npx vitest run tests/engine/evidence-traces.test.ts passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Every failing verdict can cite exact events, states, and rules through a persisted structured evidence snapshot
- Explanation text is generated deterministically from evidence fields
</success_criteria>

<output>
After completion, create .planning/phases/03-evidence-and-repair-reasoning/03-01-SUMMARY.md
</output>
